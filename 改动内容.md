# 改动内容

> 本文档汇总 Stratum 的改动记录与设计，包含：
> 1. GUI 控制台 + 后端接口补齐（合并了原《Stratum_GUI设计方案.md》《Stratum_GUI界面设计.md》）
> 2. Leader→Follower 数据同步（合并了原《改动记录_数据同步.md》）
> 3. 版本元数据摘要验证（DataSync 拉取竞态收敛）与测试覆盖补强（第十一、十二章）
>
> **约定：此后所有代码/接口/行为改动，一律追加记录到本文档，不再单独建档。**

---

## 一、背景

Stratum 对外只有 gRPC（`KnowledgeBaseService` / `QueryService` / `AdminService`，
监听 `:7000`），没有 HTTP 层、没有前端。要满足设计目标里的检测与运维要求
（健康监测、系统诊断、知识库/版本管理、检索验证），需要一个控制台。

排查后发现「全量控制台」无法直接建在现有接口上，存在两个缺口：

1. 缺少 `ListKnowledgeBases` / `GetKnowledgeBase` 两个 RPC（连文档 `[预留]` 清单里都没有），
   导致无法枚举知识库、无法查看活跃版本。
2. `GetSystemStatus` 的 `stuck_versions` / `resource_usage` 是半成品（返回空）。

本次改动补齐上述缺口，并落地了一个 Web 控制台 + 一键启动脚本。

---

## 二、交付物总览

| 类别 | 内容 |
|---|---|
| 接入层 | `cmd/stratum-gateway/`（HTTP/JSON ↔ gRPC 网关进程）+ `web/`（前端静态资源） |
| 后端接口 | 新增 `ListKnowledgeBases` / `GetKnowledgeBase`，完善 `GetSystemStatus` |
| 启动 | `start.sh` 一键启动脚本 |
| 设计 | 见下文「六、GUI 设计方案」与「七、GUI 界面设计」 |

---

## 三、文件清单

### 新增文件

| 文件 | 说明 |
|---|---|
| `cmd/stratum-gateway/main.go` | 独立网关进程：用 gRPC client stub + `protojson` 把 10 个对外 RPC 暴露为 REST `/api/*`，同源托管 `web/` |
| `web/index.html` | 控制台三页结构（总览 / 知识库 / 检索） |
| `web/style.css` | 视觉规范：状态徽标（绿/黄/红）、布局、卡片 |
| `web/app.js` | api 封装、健康轮询(5s)、版本链渲染、回滚/重建/预热、检索 |
| `start.sh` | 一键启动：构建 vecstore(C++) + Go 二进制 → 拉起 vecstore/mock-embed/stratum/gateway 四进程 |

### 修改文件

| 文件 | 改动 |
|---|---|
| `api/proto/knowledgebase.proto` | 新增 `KBStatus` 枚举、`KnowledgeBaseInfo` 消息、`ListKnowledgeBases` / `GetKnowledgeBase` 两个 RPC 及请求/响应消息 |
| `api/proto/stratum/knowledgebase.pb.go` / `knowledgebase_grpc.pb.go` | 重新生成（固定版本 `protoc-gen-go v1.34.2` / `protoc-gen-go-grpc v1.5.1`，与已提交生成物一致） |
| `internal/raft/raft.go` | `RaftNode` 接口新增 `ListKnowledgeBases(ctx) ([]KnowledgeBaseMeta, error)` |
| `internal/raft/impl.go` | `RaftNodeImpl.ListKnowledgeBases`：遍历状态机 `kbs` map |
| `internal/raft/mock.go` | `MockRaftNode.ListKnowledgeBases` 同步实现 |
| `internal/coordinator/write_test.go` / `delete_test.go` | 两个测试替身补 `ListKnowledgeBases` stub |
| `service/knowledgebase.go` | 新增 `ListKnowledgeBases` / `GetKnowledgeBase` 方法 + `kbToProto`/`indexTypeToProto`/`similarityToProto`/`kbStatusToProto` 转换 helper |
| `service/admin.go` | `GetSystemStatus` 真实填充 `stuck_versions`、`delete_failed_kbs` 与 `resource_usage`（loaded_index_count / chunk_store_bytes / doc_store_bytes） |
| `internal/index/*` | `IndexManager` 接口新增 `LoadedCount()` |
| `internal/docstore/*` | `DocStore` 接口新增 `DiskUsage()`（Pebble `Metrics().DiskSpaceUsage()`） |
| `internal/chunkstore/*` | `ChunkStore` 接口新增 `DiskUsage()`（经 vecstore `DiskUsage` RPC） |
| `vecstore/proto/vecstore.proto` + `api/proto/vecstore/*` | `ChunkStorageService` 新增 `DiskUsage` RPC，重新生成 Go proto |
| `vecstore/src/{rocksdb_storage,grpc_service}.*`、`vecstore/include/chunk_storage.h` | C++ 侧实现 RocksDB 磁盘占用统计（`rocksdb.estimate-live-data-size`） |
| `cmd/stratum/main.go` | 新增 `-data-dir`/`-grpc-addr`/`-vecstore-addr`/`-embed-addr` flag；启动时 `os.MkdirAll(dataDir)` |
| `configs/config1.yaml` | `node:` 下新增 `gateway_http_addr: "0.0.0.0:8081"` |
| `.gitignore` | 新增 `/stratum-gateway`、`/run/` |

---

## 四、关键技术决策

1. **网关走「零依赖」变体**：不引入 grpc-gateway 库、不碰 proto、不需要 protoc/buf
   生成工具链——直接用已有 `grpc` client stub + `google.golang.org/protobuf` 的
   `protojson` 手写 JSON↔gRPC 适配器。核心 `stratum` 服务一行未改。

2. **独立网关进程**：`cmd/stratum-gateway` 单独编译/部署，非阻塞拨号 `:7000`，
   后端没起来时 `/api` 返回 503，前端健康灯显示 UNHEALTHY，而不是网关卡死在启动。

3. **proto 用固定版本重新生成**：仓库生成物是 `protoc-gen-go v1.34.2` / `v1.5.1`
   （`sync.pb.go` 例外，是 v1.36.11），本次用固定版本重新生成 `knowledgebase` 相关文件，
   `sync` 文件未动，避免版本漂移产生大 diff。

4. **`EmitUnpopulated`**：protojson 默认省略零值枚举，会导致 `HEALTHY`/`HNSW`/`ACTIVE`
   这类值不输出、前端显示成 UNKNOWN；网关 marshal 加 `EmitUnpopulated: true` 保证枚举始终显式输出。

5. **`resource_usage` 实现**：`loaded_index_count` 来自 `IndexManager.LoadedCount()`（Go），
   `doc_store_bytes` 来自 Pebble `Metrics().DiskSpaceUsage()`（Go），`chunk_store_bytes`
   经新增的 vecstore `DiskUsage` RPC 取 C++ RocksDB 的 `rocksdb.estimate-live-data-size`。
   三个字段都真实填充，不再留空。

6. **`-data-dir` flag + `MkdirAll` 修复**：原来数据目录写死 `/var/lib/stratum/node1`，
   普通用户无权限；且 `NewFileWAL` 直接 `open` 文件、父目录不存在会 `fatal` 崩溃。
   加 flag 让脚本用本地目录，加 `MkdirAll` 消除崩溃。

---

## 五、接口变更

### KnowledgeBaseService 新增

| RPC | 入参 | 返回 |
|---|---|---|
| `ListKnowledgeBases` | 无 | `knowledge_bases[]`：`knowledge_base_id`、`name`、`chunk_window_size`、`chunk_overlap_size`、`index_type`、`similarity`、`embed_config`、`active_version_id`、`status` |
| `GetKnowledgeBase` | `knowledge_base_id` | 单个 `knowledge_base`（字段同上） |

### RaftNode 接口新增

```go
ListKnowledgeBases(ctx context.Context) ([]types.KnowledgeBaseMeta, error)
```

### GetSystemStatus 补全

- `stuck_versions`：遍历全部 KB 的 FAILED 版本（`updated_at` 暂用 `CreatedAt` 代理，
  因 `VersionMeta` 无独立状态更新时间字段）。
- `delete_failed_kbs`：`KBStatusDeleteFailed` 的知识库 ID 列表。
- `resource_usage`：`loaded_index_count` 已加载索引数、`chunk_store_bytes` vecstore RocksDB 大小、`doc_store_bytes` DocStore PebbleDB 大小。

---

## 六、GUI 设计方案

### 6.1 目标与范围

**目标用户**：知识库的管理者 / 运维（不是最终对话用户）。

**覆盖范围（全量控制台）**：

| 能力 | 对应功能目标 | 依赖接口 |
|---|---|---|
| 系统健康监测 | F7 运维接口 | HealthCheck / GetSystemStatus |
| 知识库生命周期 | F1 | CreateKnowledgeBase / DeleteKnowledgeBase / **ListKnowledgeBases(新增)** / **GetKnowledgeBase(新增)** |
| 版本管理 | F2 | ListVersions / CreateVersion / RollbackVersion |
| 索引运维 | F7 | RebuildIndex / WarmupVersion |
| 检索验证 | F5 | Query |

**不做**：对话/会话、prompt 构造（上层应用职责）、HybridQuery/Bleve 等 `[预留]` 能力。

### 6.2 关键发现：当前接口与「全量控制台」的缺口

对照 proto 与 `service/*.go` 实现，全量控制台**无法直接建在现有接口上**，存在 3 类缺口：

**（1）缺失的 RPC（连 `[预留]` 清单里都没有）**

| 缺失接口 | 用途 | 现状 | 影响 |
|---|---|---|---|
| `ListKnowledgeBases` | 枚举所有知识库 | 无任何 RPC 能列出全部 KB | 没有它，「知识库列表页」无法实现 |
| `GetKnowledgeBase` | 查单个 KB 元数据（name、index_type、similarity、**ActiveVersionID**、embed 配置） | 无；`ListVersions` 只给版本，`Query` 缺省 version 只回 version_id | 无法展示「活跃版本」、无法做详情页 |

> 注：文档的 `[预留]` 清单是 `GetVersion/DiffVersions/TagVersion/BatchQuery/HybridQuery/GetMetrics/GetAuditLog`，
> 而控制台真正缺的 `ListKnowledgeBases` / `GetKnowledgeBase` **不在清单里**——这是本次设计的核心发现。

**（2）已实现但「半成品」的接口**

| 接口 | 现状 |
|---|---|
| `GetSystemStatus` | `stuck_versions`、`resource_usage` 原本**返回空**（代码注释 "In a real implementation…"），仅 `health` / `wal_alerts` 有值 |
| `WarmupVersion` | 当前实现直接调用 `TriggerBuild`（注释自认是 hack），不是真正的「仅加载磁盘索引」 |

→ 运维监测页若要达到 F7 验收要求，需要补全 `GetSystemStatus` 的 `stuck_versions` 与 `resource_usage`。

**（3）检索页的向量来源**

`Query` 要求调用方传入原始向量 `vector`（Stratum 不感知向量的生成方式）。因此检索页需要：
- 直接粘贴向量（JSON 数组）——**零后端改动**；
- 或前端单独配置 embed 服务、输入文本→生成向量——需前端自带 embed 客户端（KB 的 embed 配置当前不对外暴露）。

### 6.3 技术选型对比

| 维度 | 方案一 Web + gRPC-gateway | 方案二 终端 TUI | 方案三 桌面应用 |
|---|---|---|---|
| 后端改动 | 接入层（gateway 进程） | **零** | **零** |
| 前端运行时 | 浏览器，无安装 | Go + bubbletea | Wails/Fyne 等 GUI 框架 |
| 运维友好度 | 高（可远程、可多人、可截图） | 中（终端内） | 中 |
| 与现有工具链契合 | 需引入 protoc + gateway 插件 | 高（纯 Go） | 中 |
| 推荐度 | **推荐（运维控制台首选）** | 备选（零后端改动） | 不推荐（重） |

**最终选型**：方案一，且采用**独立 gateway 进程**变体（`cmd/stratum-gateway`），
使核心 `stratum` 服务零改动、可独立部署/升级。实际实现进一步收敛为「零依赖」手写适配器
（见「四、关键技术决策」第 1 条），未引入 grpc-gateway 库。

### 6.4 总体架构

```
                浏览器 / TUI / 桌面
                       │
        ┌──────────────┴──────────────┐
        │   Gateway（方案一：独立进程）    │   ← 新增
        │   HTTP/JSON  ↔  gRPC         │
        └──────────────┬──────────────┘
                       │ gRPC (:7000)
        ┌──────────────┴──────────────┐
        │ KnowledgeBaseService         │
        │ QueryService / AdminService  │   ← 现有，零改动
        └─────────────────────────────┘
```

- 方案二/三：客户端直连 `:7000`，无 Gateway 层。
- 所有状态刷新采用**轮询**（契合设计文档「版本切换通知采用轮询而非回调」的原则），不做推送。

### 6.5 页面 / 视图清单

| 页面 | 元素 | 数据源 / 动作 |
|---|---|---|
| P1 总览 Dashboard | 健康状态灯；资源使用；长期 FAILED 版本；DeleteFailed KB；WAL 告警 | HealthCheck + GetSystemStatus，5s 轮询 |
| P2 知识库列表 | KB 卡片列表；「新建」表单；「删除」按钮（二次确认） | **ListKnowledgeBases（新增）** / CreateKnowledgeBase / DeleteKnowledgeBase |
| P3 知识库详情 / 版本链 | KB 头部；版本链（含分叉）；回滚/重建/预热按钮 | **GetKnowledgeBase（新增）** / ListVersions / RollbackVersion / RebuildIndex / WarmupVersion |
| P4 创建版本 | parent_version_id 选择；changes 多行编辑；提交 | CreateVersion（提交后轮询 ListVersions） |
| P5 检索 / 查询 | kb_id/version_id 选择；top_k/threshold/aggregation；向量输入；结果列表 | Query |
| P6 运维操作 | RebuildIndex / WarmupVersion 触发 + 结果轮询 | RebuildIndex / WarmupVersion |

### 6.6 页面 ↔ RPC 映射总表

| 页面 | 动作 | RPC | 入参（关键字段） |
|---|---|---|---|
| P2 | 新建 KB | CreateKnowledgeBase | name, chunk_window_size, chunk_overlap_size, index_type, similarity, embed_config |
| P2 | 删除 KB | DeleteKnowledgeBase | knowledge_base_id |
| P2 | 列出 KB | **ListKnowledgeBases（新增）** | — |
| P3 | KB 详情 | **GetKnowledgeBase（新增）** | knowledge_base_id |
| P3 | 版本链 | ListVersions | knowledge_base_id |
| P4 | 创建版本 | CreateVersion | knowledge_base_id, parent_version_id, changes[] |
| P3 | 回滚 | RollbackVersion | knowledge_base_id, target_version_id |
| P5 | 检索 | Query | knowledge_base_id, version_id?, vector, top_k, threshold?, aggregation |
| P1 | 健康 | HealthCheck | — |
| P1 | 系统状态 | GetSystemStatus | — |
| P3/P6 | 重建索引 | RebuildIndex | knowledge_base_id, version_id |
| P3/P6 | 预热 | WarmupVersion | knowledge_base_id, version_id |

---

## 七、GUI 界面设计

### 7.1 设计原则

1. **面向管理者/运维**：信息密度优先，操作明确可回退，危险操作二次确认。
2. **状态可视化**：一切「进行中/失败/可恢复」的状态都要有颜色 + 文案双重表达，不靠用户猜。
3. **轮询优先**：契合后端「无推送」约束，所有动态数据轮询刷新，界面明确标注刷新时间。
4. **一致性**：同一字段、同一状态、同一颜色在任何页面表达一致。

### 7.2 视觉规范

**布局框架**：

```
┌────────────────────────────────────────────────────┐
│ 顶栏：产品名 · 健康状态灯 · 环境/节点 · 刷新时间       │
├──────────┬─────────────────────────────────────────┤
│  侧边导航 │            内容区（页面切换）              │
│  · 总览   │                                         │
│  · 知识库 │                                         │
│  · 检索   │                                         │
└──────────┴─────────────────────────────────────────┘
```

**颜色语义（全局统一）**：

| 语义 | 颜色 | 用于 |
|---|---|---|
| 成功 / 就绪 | 绿 | READY、HEALTHY、操作成功 |
| 进行中 | 黄/琥珀 | PENDING、构建中、DEGRADED |
| 失败 / 危险 | 红 | FAILED、UNHEALTHY、DeleteFailed |
| 中性 / 禁用 | 灰 | 空值、禁用按钮、普通文本 |
| 强调 / 主操作 | 蓝 | 主按钮、链接、当前活跃版本高亮 |

**状态徽标（Badge）**：

| 状态 | 徽标 | 文案 |
|---|---|---|
| `IndexStatus.READY` | 绿点 | READY |
| `IndexStatus.PENDING` | 黄点（可加旋转动画） | PENDING |
| `IndexStatus.FAILED` | 红点 | FAILED |
| `HealthStatus.HEALTHY` | 绿 | HEALTHY |
| `HealthStatus.DEGRADED` | 黄 | DEGRADED |
| `HealthStatus.UNHEALTHY` | 红 | UNHEALTHY |

### 7.3 逐页设计

**P1 总览 Dashboard**：一眼看清系统是否健康、有无需要人工介入的异常。
- 顶部大卡片显示健康状态 + `details`。
- 下方卡片：资源使用 / 长期 FAILED 版本 / WAL 告警 / DeleteFailed 知识库。
- 5s 自动轮询；FAILED 版本每行提供「重建索引」快捷按钮；空区块显示灰字「无」。

**P2 知识库列表**：卡片网格（每 KB 一张卡），右上角「＋新建知识库」主按钮。
卡片显示 name、status 徽标、index_type、similarity、active_version；「打开」进详情、「删除」二次确认。

**P3 知识库详情 / 版本链**：头部显示 KB 名称 · active_version · chunk 配置 · embed。
版本链自上而下按时间排序，**支持分叉分支并列**（A/B 场景），活跃版本高亮。
每节点圆点颜色 = index_status，按状态提供操作：READY 可回滚/预热、FAILED 可重建索引、PENDING 仅展示。

**P4 创建版本**：单页表单——父版本下拉（默认活跃版本）+ changes 多行编辑
（`op` 下拉 ADD/DELETE/UPDATE、`doc_id` 必填、`content` 在 DELETE 时置灰）。
提交后返回新 version_id，跳回 P3 显示 PENDING，自动轮询。

**P5 检索 / 查询**：左表单右结果。kb_id/version_id 下拉、top_k、threshold、aggregation、
向量输入（粘贴 JSON 数组）。结果按 score 降序，顶部显示实际命中 version_id；空结果明确提示。

**P6 运维**：两列操作卡（重建索引 / 预热版本），均为异步操作，触发后 toast「已触发（异步）」。

### 7.4 关键表单 / 对话框

| 表单 | 字段 |
|---|---|
| 新建知识库 | name（必填）、chunk_window_size（默认 512）、chunk_overlap_size（默认 64）、index_type（HNSW/IVF/FLAT，后两者标「预留」置灰）、similarity、embed_config.service_addr、embed_config.model_id |
| 二次确认 | 删除 KB：「删除后异步清理所有存储数据，不可撤销」；回滚：「活跃版本将切换到 v{N}，进行中的查询不中断」 |

### 7.5 组件清单

| 组件 | 说明 | 复用位置 |
|---|---|---|
| `HealthBadge` | 三态健康灯 | 顶栏、P1 |
| `StatusBadge` | PENDING/READY/FAILED 徽标 | P3 版本节点、P1 FAILED 列表 |
| `KBCard` | 知识库卡片 | P2 |
| `VersionNode` / `VersionChain` | 版本链节点 + 分叉布局 | P3 |
| `DocChangeRow` | 变更编辑行 | P4 |
| `ResultCard` | 检索结果条目 | P5 |
| `ConfirmDialog` | 危险操作二次确认 | P2 删除、P3 回滚 |
| `PollingBadge` | 上次刷新时间 + 轮询中 | 顶栏 |
| `Toast` | 异步操作提示 | 全局 |

### 7.6 错误与边界呈现

| 场景 | 呈现 |
|---|---|
| gRPC 错误码（`internal/errors` 映射） | 统一 toast/内联错误，文案取自服务端 `status.Message` |
| 查询 PENDING/FAILED 版本 | 检索页内联红字「版本不可查询（PENDING/FAILED）」 |
| 回滚到 PENDING/FAILED 版本 | 该版本节点的回滚按钮禁用，hover 提示原因 |
| 空列表（无 KB / 无版本 / 无结果） | 灰字占位「暂无数据」 |
| 服务不可达 | 顶栏健康灯转红 UNHEALTHY + 全局错误横幅 |

### 7.7 关键交互流程

```
新建KB → CreateKnowledgeBase → 列表出现卡片
      → 打开详情 → CreateVersion(ADD 文档) → 新版本 PENDING
      → 轮询 ListVersions 至 READY → 检索页粘贴向量 → Query 返回结果
```

版本构建状态机（UI 视角）：

```
PENDING ──(轮询 ListVersions)──► READY   （可查询/可回滚）
   │
   └──────────────────────────► FAILED  （仅可 RebuildIndex）
                                    │
                          RebuildIndex ▼
                                  PENDING ──► READY
```

### 7.8 响应式与可访问性

- 布局以桌面端为主（运维控制台场景），最小支持 1280px 宽；窄屏侧边导航折叠为图标。
- 所有状态色均配文字/图标，不单靠颜色传达（色弱可用）。
- 轮询刷新不打断用户正在输入的表单（表单区与自动刷新区隔离）。

---

## 八、未实现 / 后续待办

- **`StuckVersion.updated_at`**：精确的「长期 FAILED」时长需给 `VersionMeta` 加 `UpdatedAt` 字段（状态机变更）。
- **`WarmupVersion`**：仍是近似实现（当前直接 `TriggerBuild`），真正「仅加载磁盘索引」待做。
- **文档 `[预留]` 接口**：`GetVersion`/`DiffVersions`/`TagVersion`/`BatchQuery`/`HybridQuery`/`GetMetrics`/`GetAuditLog` 本次不实现。
- **检索页向量来源**：默认「粘贴向量」零后端改动；内嵌 embed 生成向量作为可选延后。

---

## 九、验证

- 全量编译：`go build ./...` ✅
- 静态检查：`go vet ./service/... ./internal/raft/... ./cmd/...` ✅
- 全部测试：`go test ./... -timeout 180s -count=1`（17 包全过）✅
- 网关冒烟：静态资源 200；后端离线时 `/api/*` 返回 503（非 404）、路由无冲突 ✅
- 端到端（`start.sh` 拉起后经网关验证）：
  - `GET /api/health` → `HEALTH_STATUS_HEALTHY`
  - `POST /api/knowledge-bases` 创建 → 返回 `knowledge_base_id` + `initial_version_id`
  - `GET /api/knowledge-bases` / `/api/knowledge-bases/{id}` → 返回 name + active_version + status 等
  - `GET /api/knowledge-bases/{id}/versions` → v1 READY
  - `GET /api/system-status` → 三态 + stuck/delete-failed/wal 告警 + resource_usage

---

## 十、改动记录：Leader→Follower 数据同步

### 背景

Stratum 是多节点 Raft 集群，但 Raft 只复制元数据（KB 元数据、版本元数据、versionID），
不复制存储层数据。之前 Follower 节点只有 stateMachine 元数据，本地 DocStore、
ChunkDocMapper、VersionDocList、ChunkStore 都是空的，无法独立构建 HNSW 索引、
无法响应查询。本次改动实现了 Leader→Follower 的数据同步通道。

设计文档依据：`Stratum_设计文档v10.md` 的「多副本构建」一节——每个副本独立异步
构建自己的 HNSW 索引，前提是 Follower 本地拥有完整数据副本。

### 同步范围

**同步（Leader → Follower，gRPC 流式）**：

| 数据 | 存储 | SyncEntryType |
|---|---|---|
| DocStore 条目（含墓碑） | PebbleDB | `SYNC_ENTRY_TYPE_DOC_STORE` |
| ChunkDocMapper 正向（chunk→doc） | PebbleDB | `SYNC_ENTRY_TYPE_CHUNK_DOC_FORWARD` |
| ChunkDocMapper 反向（doc→chunk） | PebbleDB | `SYNC_ENTRY_TYPE_CHUNK_DOC_REVERSE` |
| VersionDocList 条目 | PebbleDB | `SYNC_ENTRY_TYPE_VERSION_DOC_LIST` |
| ChunkStore 向量 | Vecstore | `SYNC_ENTRY_TYPE_CHUNK_VECTOR` |

**不同步（Follower 自行构建）**：

| 数据 | 原因 |
|---|---|
| HNSW 索引 | 各副本独立构建，图结构允许有差异，召回质量等价 |
| Chunk 布隆过滤器 | 从本地 chunk store 重建 |
| 版本文档布隆过滤器 | 从本地 version doc list 重建 |

### 数据流

```
写请求 → Leader
  ├── Raft.ProposeCreateVersion → stateMachine apply（所有节点）
  ├── Leader 的 Coordinator 写本地存储（现有流程）
  └── Follower:
       handleEntryMsg 检测 cmdCreateVersion（非 proposer）
         → onVersionCreated(kbID, versionID)
           → GetClusterStatus 查 leader
           → peerAddrByID[leaderID] 查 leader 地址
           → FollowerSync.PullVersion(ctx, leaderAddr, kbID, versionID)
               ├── gRPC PullVersionData 流式接收
               ├── 写入本地 DocStore / ChunkDocMapper / VersionDocList / ChunkStore
               └── TriggerBuild(kbID, versionID) 独立构建 HNSW
```

### 新增文件

| 文件 | 说明 |
|---|---|
| `api/proto/sync.proto` | DataSyncService gRPC 定义（`PullVersionData` server-side streaming RPC） |
| `api/proto/stratum/sync.pb.go` | 生成的 protobuf Go 代码 |
| `api/proto/stratum/sync_grpc.pb.go` | 生成的 gRPC 服务端/客户端代码 |
| `internal/sync/sync.go` | `FollowerSync` 接口 |
| `internal/sync/leader.go` | `LeaderHandler`：实现 `DataSyncServiceServer`，流式推送 5 种数据 |
| `internal/sync/follower.go` | `Follower`：流式接收 + 本地写入 + 触发 TriggerBuild |

### 修改文件

| 文件 | 改动 |
|---|---|
| `internal/pebbleutil/keys.go` | 新增 `DecodeString()` |
| `internal/docstore/pebble.go` | 新增 `DB()` 访问器 |
| `internal/chunkdoc/pebble.go` | 新增 `DB()` 访问器 |
| `internal/versiondoc/pebble.go` | 新增 `DB()` 访问器 |
| `internal/chunkstore/grpc_client.go` | 新增 `VecstoreClient()` 访问器 |
| `internal/raft/impl.go` | `PeerConfig` 加 `ServiceAddr`；`RaftNodeImpl` 加 `onVersionCreated` 回调 + `SetOnVersionCreated()`；`handleEntryMsg` 中 Follower 检测 `cmdCreateVersion` 触发回调 |
| `cmd/stratum/main.go` | 创建 `LeaderHandler` 注册到 gRPC；创建 `Follower` 绑定到 `SetOnVersionCreated`；`defaultConfig` 加 `ServiceAddr` |

### 关键设计点

1. **幂等性**：所有存储层写入按键幂等（同一 `(kbID, versionID, docID/chunkID)`
   重复写无副作用），PullVersion 重复调用安全。

2. **Leader 直传**：当前规模（几十到上百知识库）下 Leader 直传足够，Follower
   之间对等分发留作未来优化。

3. **访问器暴露底层句柄**：`LeaderHandler` 需要直接扫 PebbleDB、读 Vecstore，
   因此给三个 Pebble 实现和 ChunkStore 加了 `DB()` / `VecstoreClient()` 访问器。

4. **ServiceAddr 区分**：`PeerConfig` 新增 `ServiceAddr`（Stratum 层 gRPC 地址），
   与 `RaftAddr`（Raft RPC 地址）区分，用于数据同步连接。

### 未实现 / 后续待办

- **孤儿 chunk GC**：`ChunkDocMapper.DeleteByDoc`、`ChunkStore.Delete` 接口已
  预留，但未接入删除路径。
- **WAL 恢复逻辑**：`Recover` 返回的 pending record 处理逻辑未实现。
- **布隆过滤器真实实现**：`main.go` 中 chunk/version bloom 仍用 Mock 占位。

### 验证

- 全量编译通过：`go build ./cmd/stratum/`
- 全部测试通过：16 个 internal 包 + service + integration（含 3 节点 Raft 集群测试）

---

## 十一、改动记录：版本元数据摘要验证（DataSync 拉取竞态收敛）

### 背景

DataSync（第十章）采用"follower 在 raft apply 到 `cmdCreateVersion` 后主动向
leader 拉取存储层数据"的架构，但 leader 的写路径是：

```
WAL.BEGIN → Raft.ProposeCreateVersion（阻塞到 raft apply，只提交元数据）
          → 返回后才写存储层（split/embed/ChunkStore/DocStore/...）
          → WAL.COMMIT → TriggerBuild
```

follower 在 raft 提交后即可触发拉取，可能早于 leader 存储写入完成——此时
`PullVersionData` 读到空版本直接返回，follower 本地存储落空、并构建一次空索引；
而版本随后被标为 READY，无任何机制补拉，**副本数据永久分叉（静默）**。

排查确认：这是现有设计（v9 `ProposeCreateVersion` 只带 `kbID + parentVersionID`、
v10 写路径时序、sync.proto "四层存储数据不通过 Raft 复制"）的固有时间窗，不是
实现偏离。**不采用"数据随日志/元数据传递"的方案**（会把数据负荷转嫁给元数据
通道，违背分层设计）；也不改架构，在架构内用"内容自证"消除竞态。

### 方案

给版本元数据增加一个**文档 ID 集合摘要**，作为"leader 存储写入已完成"的
可验证标志：

1. **摘要定义**：`DocIDSetHash = SHA-256(排序后的全量文档 ID，'\n' 连接)`。
   排序使摘要与插入顺序无关，leader 与 follower 对同一集合必然算出相同值。
2. **leader 提交时机**：`WriteCoordinator.Execute` 在 `WAL.WriteCommit` 之后、
   `TriggerBuild` 之前，用新命令 `cmdUpdateVersionSummary` 随 raft 提交摘要
   （此时 leader 存储数据必已全部落盘）。提交失败非致命：版本无摘要时 follower
   退化为"本地有数据即成功"。
3. **follower 验证重试**：`onVersionCreated` 回调改为——拉取 → 用本地
   VersionDocList 重算摘要 → 与元数据里的摘要比对 → 不匹配（含拉到空版本）
   则指数退避重试，直到匹配或超时。初始版本（v1，CreateKB 创建、无变更文档、
   永不产生摘要）拉一次即完成，不做验证循环。
4. **收敛性**：follower 一旦从 raft 看到摘要，leader 数据必然已落盘，重试必然
   拉到完整数据；follower 拉取得再早也无害——摘要比对失败只会触发重试。
   竞态从"依赖时序巧合"变为"内容驱动的最终一致"。

### 新增文件

| 文件 | 说明 |
|---|---|
| `internal/sync/summary.go` | `ComputeDocIDSetHash`（摘要计算，供 leader/follower 共用）+ `VerifyDocIDSet`（follower 本地比对） |
| `internal/sync/summary_test.go` | 摘要确定性/排序无关/空集/交叉验证 + VerifyDocIDSet 全路径 |

### 修改文件

| 文件 | 改动 |
|---|---|
| `internal/types/types.go` | `VersionMeta` 新增 `DocIDSetHash string` 字段（含语义注释） |
| `internal/raft/command.go` | 新增 `cmdUpdateVersionSummary` 命令类型与 `DocIDSetHash` 载荷字段 |
| `internal/raft/state_machine.go` | `apply` 新增 `cmdUpdateVersionSummary` 分支（版本不存在报 `ErrVersionNotFound`） |
| `internal/raft/raft.go` | `RaftNode` 接口新增 `ProposeUpdateVersionSummary(ctx, versionID, docIDSetHash)` |
| `internal/raft/impl.go` | `RaftNodeImpl` 实现 `ProposeUpdateVersionSummary` |
| `internal/raft/mock.go` | `MockRaftNode` 同步实现 |
| `internal/coordinator/write_impl.go` | `writeVersionDocList` 返回排序后的全量 docID；`Execute` 在 COMMIT 后计算并 propose 摘要（非致命） |
| `cmd/stratum/main.go` | `onVersionCreated` 改为"摘要验证 + 指数退避重试"循环；新增 `verifyVersionPull` 辅助；v1 特判 |
| `internal/coordinator/write_test.go` / `delete_test.go` | 测试替身 `testRaftNode` / `deleteTestRaftNode` 补 `ProposeUpdateVersionSummary` |
| `integration/e2e_test.go` | 双节点测试回调改为摘要验证重试；单机/双节点新增"元数据摘要 == 本地重算"断言 |

### 关键设计点

1. **内容自证，而非时序保证**：摘要不依赖"谁先谁后"，只依赖"元数据里的摘要
   与本地存储重算是否一致"。follower 拉取得早没关系，最终收敛。
2. **数据负荷不进元数据**：元数据只多一个几十字节的哈希，文档/向量/chunk 仍
   走 DataSync 通道，架构不变（经确认）。
3. **空集合有确定摘要**（SHA-256 空输入），与"未提交摘要（空串）"可区分——
   "删空版本的摘要"与"leader 还没 propose"不会混淆。
4. **摘要 propose 非致命**：失败/遗漏时 follower 退化为"本地非空即成功"，
   不阻塞写路径，行为不劣于改动前。
5. **初始版本 v1 特判**：v1 由 CreateKnowledgeBase 创建、无变更文档、永不产生
   摘要，跳过验证循环（否则会重试到超时）。

### 未实现 / 后续待办

- 摘要验证重试耗尽时的可观测性：`GetSystemStatus` 尚未暴露"拉取重试中/未收敛"
  的版本状态（当前仅 leader 侧与 follower 侧日志）。
- 崩溃恢复路径：若崩溃发生在"WAL COMMIT 之后、摘要 propose 之前"，重启后该
  版本无摘要，靠退化分支兜底；后续可考虑恢复时补 propose。

### 验证

- 全量测试（`go test -race ./...`）通过。
- 单机端到端 `TestRealStack_FullLifecycle`：v2 READY 后断言元数据摘要非空且与
  本地 versiondoc 重算一致。
- 双节点端到端 `TestRealStack_TwoNodeReplication`：断言 leader/follower 元数据
  摘要一致，且与 follower 本地重算一致（即"内容自证"跨副本成立）。
- 单机 `TestWriteCoordinator_ProposesVersionSummary`：写路径提交的摘要 ==
  versiondoc 全量集合的摘要。
- 单元：`TestStateMachine_Apply_UpdateVersionSummary`、`TestRaftNodeImpl_ProposeUpdateVersionSummary`、
  `TestComputeDocIDSetHash_*`、`TestVerifyDocIDSet_*`。

---

## 十二、补记：此前未记录的修改（测试覆盖补强与缺陷修复）

> 本会话期间完成、但早于第十一章的改动，补记于此。

### 测试覆盖补强（对应《Stratum_测试顺序.md》第一、二批）

| 包 | 覆盖率变化 | 新增内容 |
|---|---|---|
| `internal/types` | 0% → 100% | `types_test.go`：String() 全分支、iota 默认值语义 |
| `internal/sync` | 0% → 86% | `leader_test.go` / `follower_test.go`：LeaderHandler 全量流式/MVCC/墓碑/前缀隔离；Follower applyEntry 全类型/幂等/失败路径；leader↔follower 真实 gRPC 联动 |
| `service` | 57% → 89% | `admin_test.go`（HealthCheck 三态/GetSystemStatus/RebuildIndex/WarmupVersion）、List/GetKnowledgeBase、proto 转换全分支、aggregate 全方法 |
| `internal/raft` | 60% → 87% | `state_machine_test.go`（apply 全命令/约束/快照往返）、`impl_extra_test.go`（KB 生命周期/快照压缩） |
| `internal/kvraft` | 65% → 76% | `snapshot_test.go`（快照压缩往返/书签方法） |
| `internal/coordinator` | 77% → 83% | `retry_test.go`（成功/耗尽/ctx 取消） |
| `internal/pebbleutil` | 70% → 100% | DecodeString/EncodeVersionID 全路径 |

### 缺陷修复

| 缺陷 | 修复 |
|---|---|
| sync 向量编码用数值转换（`uint32(f)`）破坏位模式（0.5→0、负值→4.29e9） | `float32sToBytes`/`bytesToFloat32s` 改用 `math.Float32bits/frombits` |
| sync 把 docstore 内部 tag 字节（0x01/0x00）当语义内容导致同步后文档带前缀字节 | wire 保留 tag（`{0x01}+内容` / `{0x00}` 墓碑），follower 侧 `applyDocStoreEntry` 解码——保留"空内容文档 vs 墓碑"的区分（经确认采用原有 tag 标记方案） |
| `cmd/stratum/main.go` 未调用 `IndexManager.SetBuildDataSources`（真实服务首次 CreateVersion 建索引即 nil 调用崩溃） | main.go 补装配（versiondoc/chunkdoc/vecstore 读向量），并导出 `chunkstore.EncodeKey` |
| kvraft `TestCluster_ElectsExactlyOneLeader` 断言过紧（leader 刚选出时 follower term 未同步的时序 flake） | term 断言改为轮询等待收敛 |
| 双节点测试中间轮空索引构建 flake | 查询断言改为"最终一致轮询直到可检索"（配合第十一章方案后由摘要验证收敛） |

### 验证

- `go test -race ./...` 全量通过；真实 vecstore 端到端（`integration/e2e_test.go`）
  依赖 `build/vecstore/vecstore_server`（缺失时自动 skip）。

---

## 十三、改动记录：索引构建分批发送（修复 4 MiB gRPC 消息超限）

### 背景

用 `scripts/gen_test_data.py`（本会话新增，经 REST 网关生成多知识库大规模
测试数据）以默认大参数（每知识库 2000 篇文档、单批 1000 篇）运行时，大量版本
索引构建失败，`stratum` 日志报：

```
index build failed ... ResourceExhausted: Received message larger than max (6240855 vs. 4194304)
```

根因：`internal/index/impl.go` 的 `IndexManagerImpl.build()` 把该版本**全部
chunk 向量一次性打包进一个 `BuildIndexRequest`** 发给 vecstore，超过 gRPC 默认
4 MiB（4194304 字节）消息上限，被 vecstore C++ 服务拒收。1000 篇文档（窗口
512、每篇约 2~3 个 chunk、768 维 float32）≈ 6~9 MB 向量，必然超限；此前测试
数据量小（几十篇 ≈ 数百 KB）未触发。失败只影响异步索引构建，存储层数据
（docstore / chunkstore / 版本元数据）在此之前已同步写入，版本标记为 FAILED、
不可查询。

### 方案

按"服务端分批发送"解决（而非简单调大 gRPC 上限），使数据量再大也只增加批数、
永不超限：

1. **proto 新增 `AddChunks` RPC**（`vecstore.proto` 的 `VectorIndexService`），
   语义为"向 (kb_id, version_id) 的索引增量追加 chunk 向量"。
2. **Go 侧分批**：`build()` 读向量时按字节预算（2 MiB）切批，第一批走 `Build`
   （全量重建），后续批走 `AddChunks`（增量追加）。
3. **C++ 侧增量实现**：`HNSWVectorIndex::AddChunks` 首次调用时按当前 metric
   建索引（等价 Build），后续 `faiss::IndexHNSWFlat::add` 追加并同步扩展
   `id_to_chunk_id_` 侧表；`Build` 重构为 `Reset() + AddChunks()`。
4. **空版本仍调一次 `Build`**：保持"分批前 `Build(empty)` 会在 vecstore 侧建立
   (kb, version) 索引条目"的原语义，否则空版本的后续 Search 会报
   "no index built or loaded"。

### 接口变更

| RPC | 入参 | 返回 |
|---|---|---|
| `VectorIndexService.AddChunks`（新增） | `kb_id`、`version_id`、`chunks[]`（`ChunkVectorProto`） | 空 |

### 修改文件

| 文件 | 改动 |
|---|---|
| `vecstore/proto/vecstore.proto` | `VectorIndexService` 新增 `AddChunks` RPC + `AddChunksRequest`/`AddChunksResponse` 消息 |
| `api/proto/vecstore/vecstore.pb.go` / `vecstore_grpc.pb.go` | 重新生成（固定版本 `protoc-gen-go v1.34.2` / `protoc-gen-go-grpc v1.5.1`，与已提交生成物一致） |
| `internal/index/impl.go` | `build()` 改为按 2 MiB 预算分批（第一批 `Build`、后续 `AddChunks`）；新增 `collectChunkBatches` 切分逻辑；空版本仍调一次 `Build` |
| `internal/index/index_test.go` | `mockVectorIndexClient` 补 `AddChunks` 并加调用计数；新增 `TestIndexManager_BuildBatchesChunks`、`TestIndexManager_BuildEmptyVersionStillCallsBuild` |
| `vecstore/include/vector_index.h` | `VectorIndex` 接口新增 `AddChunks` 纯虚方法（含分批非原子性的语义注释） |
| `vecstore/src/hnsw_index.h` / `hnsw_index.cpp` | `HNSWVectorIndex` 声明并实现 `AddChunks`（首次建索引、后续增量 add）；`Build` 重构为 `Reset()+AddChunks()` |
| `vecstore/src/grpc_service.h` / `grpc_service.cpp` | `VectorIndexServiceImpl` 新增 `AddChunks` handler |

### 关键设计点

1. **预算取上限一半**：单条 RPC 预算 `maxBuildMessageBytes = 2 MiB`，gRPC 默认
   上限 4 MiB，给 `chunk_id` 与 protobuf 序列化开销留出余量。切分按 chunk 粒度，
   估算 `4*dim + len(chunk_id) + 64` 字节/chunk。
2. **第一批 Build、后续 AddChunks**：`Build` 保留"全量重建"语义（含 metric 与
   `Reset`），`AddChunks` 只追加，不重复归一化判定逻辑（复用 `metric_`）。
3. **C++ 侧表一致性**：`id_to_chunk_id_` 与 Faiss 插入顺序严格对应（增量 add
   时 push_back 到尾部），Save/Load 的 sidecar 顺序不受影响。
4. **分批非原子性**：分批期间并发 `Search` 可能读到部分构建的索引；产品路径由
   Go 侧 `IndexManagerImpl` 的 `loading`/`loaded` 门控保证构建完成后才查询，直接
   调用 vecstore 的调用方需自行保证顺序（已在接口注释说明）。
5. **单 chunk 超限**：预算按 chunk 粒度切分，若单个 chunk 向量本身已超过 2 MiB
   （现实中 embedding 维度远达不到）该批会如实超限，注释已如实说明。

### 未实现 / 后续待办

- 未给 vecstore 侧加 per-key "building" 状态让并发 Search 等待（当前靠 Go 侧
  门控兜底）；若未来出现绕开 IndexManager 直接调 vecstore 的路径，需补上。

### 验证

- `go test ./... -timeout 180s -count=1` 全量通过（17 包，含
  `integration/TestRealStack_TwoNodeReplication` 两节点复制回归）。
- C++ `ctest --test-dir build` 18/18 通过；`cmake --build` 无警告。
- 端到端：重新编译二进制后用 `scripts/gen_test_data.py --docs 1000`（此前必超限
  的量级）跑通 PASS 136 / FAIL 0，`stratum` 日志 0 个 ResourceExhausted。
- 新增单元测试覆盖：多批切分（600 chunk → 1×Build + 2×AddChunks）、空版本
  Build-once。

---

## 十四、改动记录：预热改走 PENDING 并补完成反馈

### 背景

控制台「预热」按钮（`web/app.js` 的 `warmup()`）此前只提示「已触发预热（异步）」，
存在两个问题：

1. **无法感知预热结果**：`WarmupVersion`（`service/admin.go`）不把版本状态置为
   PENDING（与 `RebuildIndex` 不同），前端也不刷新/轮询，用户无法得知预热成功
   还是失败；成功时状态仍是 READY（与预热前无差别），失败时状态变 FAILED 也
   只能靠手动刷新发现。
2. **预热未完成时回滚存在竞态**：预热实际是异步重建索引（`TriggerBuild` →
   重新 `Build`/`AddChunks`），期间版本状态保持 READY，`RollbackVersion` 的校验
   （目标版本非 PENDING、非 FAILED）会放行。若重建随后失败，活跃版本会是一个
   FAILED 版本：查询被 `FailedPrecondition "version index is FAILED"` 拒绝（整个
   知识库不可查询），且 FAILED 版本不能再作为回滚目标或父版本，只能手动重建修复。

### 方案

让预热与重建走同一条「置 PENDING → 异步构建 → 回调置终态」链路，并复用前端
已有的 PENDING 自动轮询做完成反馈：

1. **后端置 PENDING**：`WarmupVersion` 先
   `ProposeUpdateVersionStatus(versionID, IndexStatusPending)`，再
   `TriggerBuild`（与 `RebuildIndex` 完全对齐）；构建完成后由
   `BuildCompleteCallback`（`cmd/stratum/main.go` 注册，经 raft 写回状态）置为
   READY/FAILED。
2. **前端完成反馈**：`warmup()` 记录 `warmupWatchVersion` 并延时 1 秒刷新版本
   列表；`loadVersions()` 复用的 PENDING 轮询（3 秒一次，READY/FAILED 后停止）
   检测到被预热版本离开 PENDING 时，toast「预热完成：vX READY」或「预热失败：
   vX FAILED」。
3. **避免误报**：`rebuild()` 清空 `warmupWatchVersion`，防止预热中又手动重建时
   把重建结果当作「预热完成」上报。

### 修改文件

| 文件 | 改动 |
|---|---|
| `service/admin.go` | `WarmupVersion` 先置 PENDING 再 `TriggerBuild`；更新过时注释（原「dummy vector Search 触发加载」与实现不符） |
| `service/admin_test.go` | 重写预热 3 个用例：预热后状态必须为 PENDING；版本不存在时状态更新失败报错；`TriggerBuild` 失败报错（改为真实版本存在路径，仿照 `RebuildIndex` 测试） |
| `web/app.js` | 新增 `warmupWatchVersion`；`warmup()` 记录 watch + 延时刷新；`loadVersions()` 完成 toast；`rebuild()` 清 watch |

### 关键设计点

1. **回滚竞态被 PENDING 校验天然拦截**：预热期间版本为 PENDING，`RollbackVersion`
   返回 `FailedPrecondition "target version is PENDING"`，「回滚到正在重建、可能
   变 FAILED 的版本」不再可能。
2. **预热期间版本不可作为父版本**：`CreateVersion` 对 PENDING 父版本报
   `ErrInvalidParentVersion`，预热窗口（通常几秒）内该版本暂不能派生新版本。
3. **成功/失败反馈不对称**：失败有独立状态（FAILED 红徽章 + 重建按钮），成功
   状态回到 READY——toast 是成功路径上唯一的显式提示；这是复用现有
   PENDING/READY/FAILED 状态机的最小改动。
4. **置 PENDING 的容错**：若该版本已有构建在进行（`TriggerBuild` 的 `loading`
   去重直接返回 nil），原构建完成时回调仍会置终态，不会卡在 PENDING。

### 未实现 / 后续待办

- **LRU 参数 WEB 控制**（运维界面调整 `lru_capacity`，当前为进程内配置、默认 16、
  仅 `configs/config1.yaml` 可静态改）——已与用户讨论过三种方案（仅运行时生效 /
  写回配置文件 / raft 集群同步），用户暂缓，未实施。
- **vecstore 侧内存真正释放**：Go 侧 LRU 逐出只删 `loaded` 登记，vecstore 侧
  HNSW 索引对象不会卸载（全代码库无 `Reset` 调用点）；调小 LRU 容量不会真正
  释放 vecstore 内存，需要逐出时调 `Reset`（跨 C++/proto/Go 改动）。
- 预热语义上仍是「重建索引」（读全部 chunk 向量重新 `Build`），并非真正的
  「加载已存在索引」；若要做轻量加载需新增专用 `LoadIndex` 路径。

### 验证

- `go build ./...` 通过。
- `go test ./service/ -run 'Warmup|Rebuild' -v`：3 个 Warmup + 3 个 Rebuild 用例
  全部 PASS（含置 PENDING 断言）。
- `go test ./service/ ./internal/index/ ./internal/raft/` 全量通过，无回归。
- `go test ./integration/ -run 'Warmup|Rebuild' -v`：`TestIntegration_WarmupVersion`
  与 `TestIntegration_RebuildIndex` 均 PASS。
- `node --check web/app.js` 语法检查通过。

---

## 十五、改动记录：Docker 三节点集群测试（T4 落地）

### 背景

测试文档《Stratum_测试顺序.md》第四批要求 Docker Compose 三节点集群验证，但
代码当时不具备可运行条件：

1. `cmd/stratum/main.go` 无 `-config` 参数，`NodeID / RaftAddr / Peers` 硬编码为
   单节点（`defaultConfig()`），无法启动第 2/3 个节点；而
   `integration/docker/Dockerfile` 的 ENTRYPOINT 又是 `stratum -config ...`，直接
   `flag provided but not defined` 退出。
2. `integration/docker/` 缺少 `config2.yaml` / `config3.yaml`（compose 引用了）。
3. `docker_test.go` 硬编码向 node1 写，假设 node1 是 leader（Raft 随机选举）。
4. 空版本 Query 报 `ErrIndexNotReady`（初始版本无索引条目）。
5. `integration/e2e_test.go` 编译失败：标准库 `sync` 与 `stratum/internal/sync`
   包名冲突。
6. kvraft 默认选举超时 `[150ms, 300ms)` 在 Docker 网络下导致永久 split-vote。

### 方案与改动

| 文件 | 改动 |
|---|---|
| `cmd/stratum/main.go` | 新增 `-config` 参数、`fileConfig` 结构与 `loadConfig()`：YAML 叠加 `defaultConfig`，命令行 flag 优先级最高；`appConfig` 增加 Raft 三项时序并传入 `NewRaftNodeImpl` |
| `cmd/stratum/main_test.go` | 新增 `loadConfig` 单测（叠加默认值 / 未设字段回落） |
| `integration/docker/config2.yaml` / `config3.yaml` | 新建；三份 config 统一补 `raft.peers.service_addr` 与 `embed` 段 |
| `integration/docker/Dockerfile` | 改为单阶段 `FROM alpine:latest` + COPY 宿主机预构建静态二进制（本环境 Docker Hub 与镜像加速器均不可达，无法用 golang 构建镜像） |
| `integration/docker/docker-compose.yml` | mock-embed 改用宿主机预构建二进制；移除过时 `version` 字段 |
| `integration/docker/docker_test.go` | 改为向各节点重试定位 leader 再写入，验证三节点一致性 + 空版本查询 |
| `service/query.go` | Query 遇 `ErrIndexNotReady` 且版本确实为空时返回空结果而非报错 |
| `integration/e2e_test.go` | 标准库 `sync` 加别名 `stdsync` 解决包名冲突 |
| `go.mod` / `go.sum` | 引入 `gopkg.in/yaml.v3` |
| `.gitignore` | 忽略 docker 预构建二进制 `/integration/docker/stratum`、`/integration/docker/mock-embed` |

### 关键设计点

1. **Raft 时序显式加宽**：`HeartbeatInterval=200ms`、`ElectionTimeoutMin/Max=
   2000/4000ms`（与 `cluster_test.go` 进程内测试一致）。kvraft 默认值是为进程内
   测试调的，Docker 网络下会产生永久 split-vote（term 无限递增、反复 stepping
   down）。
2. **环境适配**：无法拉取 golang/alpine 基础镜像（registry 与 mirror 均 DNS
   失败），改为宿主机 `CGO_ENABLED=0` 构建静态二进制，再 COPY 进本地已有的
   `alpine:latest`。

### 验证

- `go test ./... -timeout 180s -count=1`：24 个包全部 ok。
- `go test ./integration/... -run TestMultiNode -v`：进程内 3 节点 PASS。
- `docker compose up -d --build` + `go test ./integration/docker/... -tags=docker`：
  `TestT4_MultiNode_Consistency` PASS。

---

## 十六、改动记录：集群边界测试（节点宕机/故障转移/重启恢复）

### 背景

T4-2 容错用例（少数派故障、leader 宕机恢复、节点重启恢复）此前全部 `t.Skip`。
补齐为真实 `docker kill` / `docker start` 故障注入测试，并在此过程中发现一个
真实缺陷：follower 重启重放日志时，`onVersionCreated` 数据同步回调同步阻塞了
raft apply 循环。

### 方案与改动

| 文件 | 改动 |
|---|---|
| `integration/docker/docker_test.go` | 新增 `TestT4_MinorityFaultTolerance`、`TestT4_LeaderFailover`、`TestT4_NodeRestartRecovery`；新增 `composeCmd` / `killNode`(SIGKILL) / `startNode` / `waitForLeader` / `nodeSeesKB` 等故障注入与 leader 探测 helper；KB 名用时间戳保证可重复运行 |
| `internal/raft/impl.go` | `handleEntryMsg` 中非本节点提案的 `CreateVersion`，其 `onVersionCreated` 回调改为 `go` 异步执行 |

### 关键设计点

1. **apply 循环不再被数据同步阻塞**：follower 重启后重放历史日志，每条
   `CreateVersion` 触发 `onVersionCreated`（`PullVersion`：dial + stream + 重试，
   最长 30s）。同步调用会卡死 apply 循环，后续日志（含 KB 元数据）无法及时
   apply——`TestT4_NodeRestartRecovery` 首次运行即因此 40s 超时。改为异步后该
   用例 2.27s 通过。
2. **Raft 不转发写**：测试须先探测 leader（无 leader 查询 RPC），逐节点重试
   写入定位。

### 验证

- `go test ./integration/docker/... -tags=docker -v -timeout 300s`：
  `Consistency` 2.07s、`MinorityFaultTolerance` 1.40s、`LeaderFailover` 5.37s、
  `NodeRestartRecovery` 2.27s 全部 PASS。
- `go test ./... -count=1` 无回归。

---

## 十七、改动记录：数据量测试 + 集群接入真实 vecstore

### 背景

在 Docker 集群上做数据量测试（对应 T3-4 CreateVersion 数据量 / T4-4 存储效率）。
但集群的 `vecstore.grpc_addr` 此前指向 mock-embed（HTTP），索引构建必然失败
（`Build RPC: http2: frame too large`），无法真实写入。

### 方案与改动

| 文件 | 改动 |
|---|---|
| `integration/docker/config1/2/3.yaml` | `vecstore.grpc_addr` 改为 `host.docker.internal:7100`（宿主机真实 vecstore_server） |
| `integration/docker/docker-compose.yml` | 三节点加 `extra_hosts: host.docker.internal:host-gateway` |
| `integration/docker/datavolume_test.go` | 新增 `TestT4_DataVolume`：灌入可配置数量文档（`STRATUM_VOLUME_DOCS`，默认 1000），采样 CreateVersion 耗时、索引构建耗时、各节点与 vecstore 存储占用、Query 结果数 |
| `.gitignore` | 忽略 `/vecstore_rocksdb/`（vecstore 误启动残留的运行数据） |

### 指标结果（默认 1000 篇文档 ≈ 2000 chunk，768 维）

| 指标 | 实测 | 文档目标 |
|---|---|---|
| CreateVersion 1000 篇文档 | 17.0s | T3-4：< 30s ✅ |
| 索引构建 ready | 0.5s | — |
| Query（top-k=10） | 返回 10 条 | — |
| 存储（leader） | 3.84 MiB | — |
| 存储（两 follower） | 各 0.25 MiB（采样时 data sync 尚未完成） | — |
| 存储（vecstore RocksDB） | 0.25 MiB | — |

### 验证

- `STRATUM_VOLUME_DOCS=50` 冒烟：PASS（链路打通）。
- 默认 1000 文档：PASS（18.06s）。
- `go vet -tags=docker ./integration/docker/` 通过。

### 说明 / 后续待办

- 存储指标仅 1000 文档采样，不能直接对标 T4-4 的「100 万 chunk < 10GB」；
  可用 `STRATUM_VOLUME_DOCS=100000` 放大规模实测。
- follower 存储采样早于异步 data sync 完成，看副本均衡需先等同步收敛（当前无
  现成同步完成信号）。


---

## 十六、改动记录：进程内三节点集群测试（T4 补强）

### 背景

第四批（T4）正式验收测试位于 `integration/docker/`：带 `//go:build docker`
标签、需要 docker daemon（当前环境不可用，从未运行过），且其中
`TestT4_MinorityFaultTolerance` / `TestT4_LeaderFailover`（T4-2）、
`TestT4_PerformanceBaseline`（T4-3）、`TestT4_StorageEfficiency`（T4-4）均为
`t.Skip` 空壳。此前进程内可验证的集群行为只有 kvraft 层（`internal/kvraft/
cluster_test.go`）、raft 元数据层（`TestMultiNode_RaftNodeImpl_3Node`）与双节点
完整栈测试。本次补一个**不依赖 docker 的进程内三节点完整栈**测试，覆盖
T4-1 与 T4-2 的进程内可测部分。

### 新增 / 修改文件

| 文件 | 改动 |
|---|---|
| `integration/cluster_e2e_test.go`（新增） | `TestRealStack_ThreeNodeCluster_FaultTolerance`：3 个完全真实的节点（真实 Pebble 存储 + FileWAL + 独立 vecstore 子进程 + 真实 IndexManager + 真实 Raft + 真实 coordinator/service + 摘要验证 DataSync 拉取）组成一个 Raft 集群 |
| `integration/e2e_test.go`（修改） | `realNode` 支持调用方指定的持久化 `baseDir`（新增 `newRealNodeWithAddrsAndDir`）与幂等 `Stop()`（`sync.Once`，关停 gRPC/raft/索引/vecstore/存储/WAL），用于模拟宕机与同目录重启；抽出公共 helper：`wireSyncPull`（动态解析 leader 地址 + 摘要验证重试）、`leaderAddrOf`、`verifyFollowerPull`、`waitQueryDoc`（最终一致读轮询）；双节点测试改用 `wireSyncPull` |

### 测试场景（对应 T4-1 / T4-2）

| 场景 | 步骤 | 结果 |
|---|---|---|
| T4-1 多副本一致性 | leader 写 KB + v2 → 3 节点全部 READY → 每节点查询命中相同文档 | 通过 |
| T4-2 少数派故障 | 停 1 个 follower → leader 写 v3 → 幸存节点查询命中 | 通过 |
| T4-2 节点重启恢复 | 停掉的 follower 从同一 baseDir/地址重启 → 追上 v3 数据并查询 v2/v3 | 通过 |
| T4-2 leader 切换不丢数据 | 停 leader → 幸存 2 节点重选新 leader → 新 leader 查询 v2/v3 历史数据 → 新 leader 写 v4 | 通过 |
| T4-2 旧 leader 重启恢复 | 旧 leader 同目录重启 → 追到 v4 READY → 3 副本最终一致 | 最后一步未通过（见下） |

故障序列经调整保证每步多数派存活（≥2/3）：先停 follower → 重启 → 再停
leader，避免 3 节点在 2 个故障下失去多数派（首次运行即暴露此序列设计问题并修正）。

### 测试状态与遗留

- 场景 1-4 及场景 5 的"追日志到 v4 READY"全部通过；**最后一步失败**：
  旧 leader 重启后立即查询 v4，`Query` 返回 `FailedPrecondition: index not ready`
  （版本 READY 来自 raft 状态复制，而本地索引构建是异步的，重启节点尚未完成
  构建），`waitQueryDoc` 目前只对"空结果"重试、对错误直接 `t.Fatalf`。
  属**测试侧等待逻辑问题，非生产缺陷**（`IndexManager.acquire` 对构建中的版本
  会等待；此处是构建尚未触发的窗口）。
- 修复方向（留待后续，按用户指示本次不再迭代）：`waitQueryDoc` 将
  `FailedPrecondition / index not ready` 等瞬时错误纳入重试即可。

### 未实现 / 后续待办

- 网络分区（docker `docker exec ... iptables`）、集群性能基准（T4-3）、存储效率
  （T4-4）仍需 docker daemon，当前环境不可用，`integration/docker/docker_test.go`
  维持 `t.Skip`。
- `TestT4_MultiNode_Consistency`（docker 版 T4-1）从未运行过（daemon 缺失）。

### 验证

- 编译与静态检查通过（`go vet ./integration/`）。
- 三节点测试单测运行：见上文"测试状态与遗留"（最后一次运行 62.9s，最后一步失败）。

---

## 十八、改动记录：独立运维控制台（/ops 控制面，多节点）

### 背景

原 Web 控制台完全依赖数据库栈：`stratum-gateway` 的所有 `/api/*` 都转发到
stratum 的 gRPC，数据库未启动时前端只能显示"连接中/UNHEALTHY"，无法做任何
运维操作。本次在**扩展现有 `stratum-gateway` 进程**（部署形态不变：每节点仍是
stratum + gateway 两个进程）的前提下，新增独立于数据库运行的控制面：

- 控制台进程（gateway）不依赖 stratum/vecstore/embed 是否启动，始终可访问；
- 每个节点的 gateway 管理**本机**三个服务（vecstore / embed / stratum）的
  生命周期（启动/停止/重启）、启动参数与日志；
- 任一节点的控制台可**跨节点**驱动整个集群：`/ops/nodes/{id}/*` 由本机
  gateway 转发到目标节点的 gateway（HTTP 同路径转发，剥掉 `/ops/nodes/{id}` 前缀）。

### 新增 / 修改文件

| 文件 | 改动 |
|---|---|
| `cmd/stratum-gateway/ops_config.go`（新增） | 控制台配置 `OpsConfig`（node_id / bin_dir / log_dir / config_dir / cluster / services），YAML 读写（默认 `run/console.yaml`）、空值默认回填、`writeStratumConfig()` 生成 cmd/stratum 兼容的启动 YAML（含 raft 定时字段） |
| `cmd/stratum-gateway/supervisor.go`（新增） | 本机子进程管理：进程组（Setpgid）启动/停止（SIGTERM→SIGKILL 宽限）/状态探测，日志重定向到 `log_dir/<svc>.log`，stratum 启动前自动重新生成配置 |
| `cmd/stratum-gateway/ops.go`（新增） | `/ops/*` 路由：health / nodes / status / start / stop / restart / config(GET/PUT) / logs/{svc} + 跨节点转发 `/ops/nodes/{id}/*`；PUT config 合并保存并热更新 supervisor |
| `cmd/stratum-gateway/main.go`（修改） | 新增 `-ops-config`、`-node-id` flag；无配置文件时物化默认 `run/console.yaml`；挂载 `/ops/` 子 mux |
| `cmd/stratum/main.go`（修改） | `fileConfig.Raft` 新增可选 `heartbeat_interval_ms` / `election_timeout_min_ms` / `election_timeout_max_ms`（0 值不覆盖默认），使控制台可编辑的 raft 定时参数真实生效 |
| `web/index.html`（修改） | 新增「运维」导航页：集群节点列表、服务启停/重启（含单服务）、启动参数编辑表单、日志查看 |
| `web/app.js`（修改） | `opsApi()` 封装、5s 轮询 `/ops/nodes`、节点选择/服务控制/参数保存/日志加载逻辑 |
| `web/style.css`（修改） | 运维页布局（ops-layout/节点卡/tab/服务行/日志查看器）样式 |
| `cmd/stratum-gateway/ops_config_test.go`、`supervisor_test.go`、`ops_test.go`（新增） | 配置读写与生成、子进程生命周期（假脚本服务）、/ops 路由含跨节点转发与日志 tail 的单测 |

### 运维 API（/ops/*）

| 端点 | 作用 |
|---|---|
| `GET /ops/health` | 控制台自身存活（与 `/api/health` 的数据库健康区分） |
| `GET /ops/nodes` | 集群节点列表 + 在线探测 + 本机服务状态 |
| `GET /ops/status` | 本机三服务状态（running/pid/started_at/log_file） |
| `POST /ops/start|stop|restart` | 本机服务启停（body `{"services":[...]}`，空=全部；启动顺序 vecstore→embed→stratum） |
| `GET /ops/config` / `PUT /ops/config` | 读取/修改并持久化本机启动参数（保存后重启生效） |
| `GET /ops/logs/{svc}?lines=N` | 本机服务日志 tail（默认 200 行，上限 5000） |
| `GET/POST/PUT /ops/nodes/{id}/...` | 跨节点：转发到目标节点 gateway（本机 id 本地分派） |

### 关键技术决策

1. **控制面与数据面同进程**：用户选定扩展现有 gateway，不新增进程。`/api/*`
   保持纯转发（数据面），`/ops/*` 为本机状态 + 进程管理（控制面），两者互不依赖。
2. **跨节点转发**：每节点 gateway 既是本机管理器又是转发入口；cluster 列表存于
   各节点 `console.yaml`，目标节点离线时返回 502，前端节点卡显示 OFFLINE。
3. **参数生效机制**：GUI 编辑的参数持久化到 `console.yaml`，stratum 每次启动时
   由 gateway 重新生成 YAML 配置（`run/configs/node<N>.yaml`）并以 `-config` 传入；
   已运行服务不受影响，重启后生效（前端有提示）。
4. **进程组管理**：子进程 Setpgid 独立进程组，停止时整组 SIGTERM，5s 宽限后
   SIGKILL，避免遗留孤儿进程（e2e 已验证停止后无残留）。
5. **默认配置物化**：gateway 首次启动自动写出 `run/console.yaml`（与 start.sh
   的 run/ 布局一致），便于查看和编辑；空值按默认回填，手写最小 YAML 即可工作。

### 验证

- `go build ./... && go vet ./cmd/...` 通过。
- `go test ./cmd/...` 全绿（含新增 3 个 gateway 测试文件：配置/生命周期/路由转发）。
- 端到端（真实二进制，临时目录）：
  1. 数据库全停时控制台在线（`/ops/health` OK，`/api/health` 503）；
  2. `/ops/start` 拉起 vecstore/embed/stratum → `/api/health` HEALTHY、知识库列表可用；
  3. `PUT /ops/config` 改 heartbeat=150ms / write max_retries=6 → 持久化 → restart
     后生成的 `node1.yaml` 含新值；
  4. `/ops/logs/stratum` 实时日志；`/ops/stop` 后 `/api` 503、控制台仍在线、无残留进程。

### 未实现 / 后续待办

- 运维 API 暂无鉴权（与现有 `/api/*` 一致，面向内网/本地）；如需暴露公网需加
  token/证书。
- 跨节点操作为同步转发（3s 超时）；大规模集群可考虑异步任务 + 轮询结果。
- 日志查看为手动刷新 tail；未做流式跟随（WebSocket/SSE）。

### 补充：start.sh 统一改由控制台管理 + 控制台优雅退出（启动方式定稿）

**`start.sh` 行为变更**：不再由脚本直接拉起四个进程，而是只启动
`stratum-gateway`（控制台进程），数据库三件套通过 `POST /ops/start` 由
控制台统一拉起。收益：

- Web「运维」页看到的状态与脚本一致（不再是"脚本起的服务，页面显示未运行"）；
- 数据库未启动时控制台依然可用（改参数、看日志、启停）；
- 首次启动按脚本目录/端口生成 `run/console.yaml`（bin_dir=run/bin、
  log_dir=run/log、data_dir=run/data/stratum、端口跟随 `STRATUM_*` 环境变量）；
  文件已存在（例如用户已在运维页改过参数）则保留，不覆盖；删除文件即可重置；
- Ctrl+C 退出：cleanup 先 `POST /ops/stop` 停服务，再终止控制台进程。

**gateway 优雅退出（防孤儿进程）**：`main()` 改用 `http.Server` +
`signal.Notify(SIGINT/SIGTERM)`，收到信号后先 `Shutdown` HTTP，再
`sup.StopAll(5s)` 停止全部托管子服务；**`main()` 等待优雅停止 goroutine
完成后再退出**（初版未等待，导致 signal goroutine 未跑完 StopAll 时
进程已退出、子进程被 reparent 残留——已修复并验证）。

验证：`start.sh` 一键启动 → `/api/health` HEALTHY；对 gateway 发
SIGTERM 后 6s 内 vecstore/embed/stratum 全部退出、无残留；仅运行
`./run/bin/stratum-gateway`（不启动数据库）时 `/ops/*` 可用、`/api/health`
返回 503（预期）。

---

## 十九、改动记录：gateway 接口改为指向路由层（删除被取代的 leader 跟随逻辑）

### 背景

外来改动引入了独立路由层 `internal/router` + `cmd/stratum-router`（gRPC
前端，写转发 leader、读负载均衡、leader 失效重发现）。此前 `stratum-gateway`
自己实现了同等的 leader 跟随逻辑（多地址直连 + `withLeaderRetry` 轮换重试），
功能重复。本次按指示把 gateway 的接口改为指向路由层，并删除 gateway 中已被
取代的多余部分。

### 改动

| 文件 | 改动 |
|---|---|
| `cmd/stratum-gateway/main.go` | gateway 简化为**单地址客户端**（只连路由层）：删除 `addrs`/`leader`/`conns`/client 数组、`cur()`、`rotate()`、`isRetryableErr()`、`withLeaderRetry()` 及逗号分隔多地址解析；`-grpc-addr` 默认改为 `127.0.0.1:7009`（stratum-router）；全部 `/api/*` 路由改为直接调用单组 `g.kb`/`g.query`/`g.admin` |
| `cmd/stratum-gateway/gateway_leader_test.go` | 删除（其测试的 `isRetryableErr`/`withLeaderRetry` 逻辑已由 `internal/router` 承担，router 有自己的单测与三节点 e2e） |
| `start.sh` | 构建并启动 `stratum-router`（`-listen 127.0.0.1:7009 -nodes 127.0.0.1:7000`），gateway 改为 `-grpc-addr` 指向路由层；新增 `STRATUM_ROUTER_ADDR` 环境变量；日志 `run/log/router.log` |
| `scripts/gateway.sh` | cluster/single 模式均改为经路由层（`-grpc-addr 127.0.0.1:7009`）；启动前检查路由层端口，未监听时打印 `stratum-router -listen ... -nodes <节点列表>` 启动提示 |
| `README.md` | 「HTTP gateway, routing layer & Web console」重写：`web UI ⇄ gateway ⇄ router ⇄ nodes` 拓扑、手动启动命令、环境变量说明 |

### 验证

- `go build ./...`、`go vet ./cmd/... ./internal/router/`、`go test ./cmd/... ./internal/router/` 全绿。
- 端到端（`start.sh` 一键）：`stratum-router`(:7009) 与 gateway 启动，
  `/api/health` HEALTHY（gateway→router→leader）；写操作（创建知识库
  `router-e2e-1`）与读操作（列知识库）均经路由层成功；终止 start.sh 后
  router/gateway/vecstore/embed/stratum 全部退出，无残留。
- 注意：正常 Ctrl+C 由 start.sh 的 trap 统一清理（含 router）；直接 kill
  gateway 进程不会连带停止 start.sh 管理的 router（需终止 start.sh 本身）。

---

## 二十、改动记录：数据量测试放大与实测（10k 文档）+ raft snapshot 阻塞发现

### 背景

第十七章「数据量测试」此前只有 1000 篇文档的实测（17.0s / 3.84 MiB）。
本次按 README 要求把测试放大并重新实测，目标 100000 篇；放大过程中发现
`TestT4_DataVolume` 三处测试侧问题并修复，另暴露一个 **kvraft snapshot
阻塞的产品缺陷**（见下）。

### 测试侧改动（`integration/docker/datavolume_test.go`）

| 问题 | 改动 |
|---|---|
| 硬编码 context 超时 15 分钟 / 构建超时 10 分钟，大文档量必超 | 环境变量化：`STRATUM_VOLUME_TIMEOUT`（默认 15m）、`STRATUM_INDEX_BUILD_TIMEOUT`（默认 10m） |
| 100000 篇单次 `CreateVersion` 请求体 ≈ 279 MB，超 gRPC 4 MiB 上限 | 分批写入：`STRATUM_VOLUME_BATCH`（默认 1000 篇/批），每批一个版本、父版本必须 READY 后链下批（PENDING parent 被 raft 状态机拒绝） |
| `duNodeBytes` 用 `docker compose exec`（docker-compose 时代遗留），集群已改原生 `docker-cluster.sh` 启动 | 改为 `docker exec`（原写法报 "service stratum-node1 is not running"） |

### 实测结果（3 节点 Docker 集群，真实 vecstore，768 维，mock embed 10ms/chunk）

| 指标 | 1000 篇（旧） | 10000 篇（本次实测） | 说明 |
|---|---|---|---|
| CreateVersion 写入 | 17.0s | 195.5s（10 批 × 1000 篇） | 与文档量近似线性 |
| 索引构建（累计） | 0.5s | 5.2s | 每批构建后等 READY |
| 存储（每节点） | 3.84 MiB（leader） | 44.5 / 52.8 / 55.0 MiB | 3 节点各自独立存储 |
| 存储（3 节点合计） | — | 152.2 MiB | |
| vecstore RocksDB（宿主） | 0.25 MiB | 4.9 MiB | 模板句循环生成 → chunk 大量去重 |
| Query top-k=10 | 10 条 | 10 条 | 全部命中 |
| 总耗时 | 18.1s | 201.6s | |

### 100000 篇受阻：kvraft snapshot 阻塞（产品缺陷，未修）

100000 篇（100 批）运行约 30 分钟后 leader 卡死、写入停摆：

1. 大批量写入使 raft 日志突破 `WithMaxLogLength` 阈值（默认 1000 条），
   kvraft 触发本地 snapshot（`requesting snapshot: log length threshold
   exceeded`，`internal/kvraft/raft.go:303`）；
2. leader 在 snapshot 流程内**永久阻塞**（14 分钟无任何心跳/日志，CPU
   持续 41%，疑似锁等待），follower 选举不出新 leader；
3. 节点上索引构建回调 `ProposeUpdateVersionStatus` 全部失败
   （`build callback retries exhausted`），版本状态永远停在 PENDING，
   `waitVersionStatus` 死等到超时。

**修复方向（未做，超出本次范围）**：
- `MaxLogLength` 已存在于 `internal/raft` 配置但**未暴露到 cmd/stratum 的
  YAML 配置**，无法绕开阈值；需在 `FileConfig.Raft` 加 `max_log_length`
  字段（默认 0 = kvraft 默认 1000），docker-cluster.sh 生成配置时按规模调大；
- 或排查 kvraft snapshot 的阻塞根因（`raft.Snapshot`/applyLoop 锁交互）。

### 验证

- `STRATUM_VOLUME_DOCS=10000` 实测：PASS（201.6s，DATA-VOLUME SUMMARY 见上）。
- `go vet -tags=docker ./integration/docker/` 通过。
- README「Testing」章节新增「Data-volume measurements」实测表。

---

## 二十一、改动记录：100000 篇压测重跑 + 快照阻塞根治（异步快照）

### 背景

第二十节记录的 100000 篇压测（旧实现）约 30 分钟后因 kvraft 快照阻塞
卡死（leader 14 分钟无心跳）。本次按「更新二进制 + docker 集群」重跑
100000 篇，过程中**连续暴露并修复 8 个问题**，最终 100 批全部写入完成、
快照 2 次触发且**无阻塞**。

### 压测中定位并修复的问题

| # | 问题 | 修复 | 文件 |
|---|---|---|---|
| 1 | `976b636` 的 `saveToDisk` 在 docker 形态下失败：节点索引目录在容器命名卷内，宿主 vecstore 按容器路径打开失败（`faiss: no such file`） | 节点数据卷由 docker 命名卷改为 **bind mount 宿主 `/var/lib/stratum/nodeN`**（容器内外同一路径）；`saveToDisk` 的 `MkdirAll` 权限改 `0777` 并显式 `Chmod`（容器 umask 022 会把 0777 收窄为 755，宿主普通用户写不了） | `scripts/docker-cluster.sh`、`internal/index/impl.go` |
| 2 | vecstore gRPC 客户端 keepalive（10s + `PermitWithoutStream: true`）被 C++ 服务端以 `too_many_pings` GoAway 踢连接，build RPC 挂起、版本永久 PENDING | keepalive 改为 60s + `PermitWithoutStream: false` | `internal/index/impl.go`、`internal/chunkstore/grpc_client.go` |
| 3 | `doBuild` 用 `context.Background()` 无超时：连接被踢后 RPC 永久挂起 | build 加 5 分钟总超时窗口，超时后明确失败而非静默卡死 | `internal/index/impl.go` |
| 4 | **快照阻塞（核心）**：log 超 `maxLogLength` 时 applyLoop 同步发快照请求；`raft.Snapshot` 持 `rf.mu` 做 `SaveSnapshot` 写盘（阻塞心跳/propose/apply）；本地快照 data 与 index 可能错位（`LastApplied()` 在 serialize 后读取已超前） | ① 快照请求**携带冻结的 `SnapshotIndex`**（`raft.go` 发送时记录）；② 状态机**深拷贝**（锁内毫秒级）后**异步** encode+写盘（锁外，apply 不阻塞）；③ `SaveSnapshot` 移出 `rf.mu`（trim 在锁内、写盘锁外，快照先于 log 落盘）；④ persister 加锁 + **防旧覆盖**（本地异步快照不覆盖更新的 InstallSnapshot） | `internal/kvraft/raft.go`、`internal/kvraft/snapshot.go`、`internal/kvraft/apply_msg.go`、`internal/raft/impl.go`、`internal/raft/state_machine.go`、`internal/kvstorage/persister.go` |
| 5 | `max_log_length` 已存在于 `internal/raft` 配置但未暴露到 YAML（第二十节遗留） | `FileConfig.Raft` 加 `max_log_length`（0 = 默认 1000）并**传入 `raft.NewRaftNodeImpl`**（曾漏传导致阈值不生效）；`docker-cluster.sh` 生成配置支持 `STRATUM_RAFT_MAX_LOG_LENGTH` 覆盖 | `cmd/stratum/main.go`、`scripts/docker-cluster.sh` |
| 6 | **共享 vecstore 多节点并发 build 竞态**（偏离设计）：3 节点共享宿主单 vecstore，leader 与各 follower 各自 `TriggerBuild` 同一版本，vecstore `Build = Reset + AddChunks` 互相清空 → 偶发 `Save: no index has been built` | 按设计文档 v11「多副本构建」改为**每节点独立 vecstore**：`docker-cluster.sh` 新增 `vecstore start/stop/status`，第 N 节点连宿主 `:710N`、RocksDB 数据目录各自独立（`run/docker/vecstore/nodeN`）；Go 代码本就按节点注入 `chunkStore`/`readChunkVector`，仅改配置即可 | `scripts/docker-cluster.sh` |
| 7 | build 瞬态失败（数据同步未收敛/并发 Reset）后版本 FAILED，靠外部重试恢复极慢（每版本失败 4-10 次、压测近乎停滞） | `doBuild` 改走 `buildWithRetry`：**仅对瞬态 gRPC 错误**（FailedPrecondition/Unavailable/DeadlineExceeded/Internal）在 5 分钟窗口内每 2s 重试，确定性错误立即失败；build 幂等，重试安全 | `internal/index/impl.go` |
| 8 | 压测 Query 阶段遗留：node1 上某版本 `loading` 标志卡 true，`acquire` 的 `sync.Cond.Wait` 永久等待（goroutine dump 确认） | **已修复**（见下文「收尾 Query 卡死的修复」）：`doBuild` 加 defer 兜底（panic 也清除 loading + Broadcast）；`waitLocked` 改有界轮询，不再依赖 cond 唤醒送达 | `internal/index/impl.go` |

### 实测结果（3 节点 Docker 集群，每节点独立 vecstore，768 维，mock embed）

| 指标 | 10000 篇（基线，本次） | 100000 篇（本次） |
|---|---|---|
| CreateVersion 写入 | 3m25s（10 批 × 1000） | **42m14s（100 批 × 1000）** |
| 索引构建（累计） | 5.1s | 2m23s |
| 总耗时 | 3m30s | **44m37s**（旧实现 30 分钟即卡死） |
| 存储（每节点） | 42.5 / 41.8 / 79.6 MiB | 608.7（leader）/ 132.0 / 136.4 MiB |
| 存储（3 节点合计） | 163.9 MiB | **877.1 MiB** |
| Query top-k=10 | 10 条命中 | 主体完成；收尾 Query 卡死（见遗留 1） |

> 写入耗时随版本号递增（每版本需重写完整 docID 集 + 构建完整索引），
> 100 批末批 ≈ 40s/批，属「每版本独立完整索引」数据模型的固有开销。

### 快照阻塞验证（核心结论）

- `max_log_length=150` 下，100000 篇压测中 **2 次触发快照**：
  `requesting snapshot: log length threshold exceeded, log_len=151`；
- 每次触发后 **log 立即 trim**（151 → 13 条）、`raft.snapshot` 持久化；
- **快照期间写入/心跳/propose 完全不停摆**：版本持续推进、无 leader 变更；
- 对比旧实现（leader 14 分钟无心跳、CPU 41%、follower 选举不出新 leader），
  **异步快照 + 写盘移出 `rf.mu` 根治了快照阻塞**。

### 收尾 Query 卡死的修复（第二十一节补充）

压测收尾时 Query 永久挂起，goroutine dump 显示 `sync.Cond.Wait` 长达数
分钟。根因与修复：

| 根因 | 修复 |
|---|---|
| `doBuild` 无兜底：若 build/`makeRoomLocked` 过程中 panic 或异常退出，`loading[key]` 永不清除，`acquire` 的 `cond.Wait` 无唤醒源 → 永久挂起（压测收尾 Query 卡死） | `doBuild` 整体改为 **defer 兜底**：defer 内 `recover()` 捕获 panic（记 FAILED）+ **必定清除 `loading` + `cond.Broadcast`**，即使构建异常也保证等待者被唤醒 |
| `waitLocked` 用 `cond.Wait` 依赖 `Broadcast` 可靠送达，唤醒丢失即挂死 | 改为 **有界轮询**（50ms 间隔，释放锁让构建推进，deadline/ctx 到期返回）——不依赖 cond 唤醒送达，无法挂死 |

验证：`internal/index` 单测通过；2000 篇冒烟（含 Query 步骤）PASS。
（100000 篇完整收尾压测未重跑——改动为确定性兜底 + 有界等待，行为可
静态论证。）

### 遗留问题（未修复）

1. **vecstore_server 二进制过期**：`run/bin/vecstore_server` 为旧 C++ 构建，
   不含 `976b636` 的 `ExistsIndex` RPC（reconcile 报 `Unimplemented`）；
   `docker-cluster.sh build` 不重建 C++ 目标，需手动
   `cmake --build run/cmake --target vecstore_server` 或脚本补上。
2. **查询路径的 vecstore 旧二进制连带**：`ExistsIndex` 不可用时 reconcile
   退化为「全部按缺失处理」，与磁盘事实推导 READY 的设计不符。

---

## 二十二、改动记录：crash recovery 重放失败不再 fatal（节点死循环根治）

### 背景

三节点 Docker 集群测试发现：当版本写在依赖故障（如 embed 不可达）后中止，
leader 本地 WAL 留下 `VERSION_ID`-without-`COMMIT` 残留；该节点重启时
`runCrashRecovery` 在 raft 尚未追平前重放，`GetKB` 报
`ErrKnowledgeBaseNotFound` → `logger.Fatal("crash recovery failed")` →
容器 restart 死循环，节点永久离线（Docker 测试中 node1 以此状态崩溃循环）。

### 方案

`runCrashRecovery`（`cmd/stratum`）三分支（VersionWrite / DeleteMark /
VersionDelete）统一改为：先做进程内有限退避重试（3 次 × 100ms 指数退避，
覆盖 vecstore/embed 等依赖短暂未就绪窗口），全部失败则 **跳过记录 +
`ReplayCounter` 计数 + 继续启动**，不再 fatal。记录保留在 WAL、下次重启
重试；数据收敛交给 Raft 日志重放 + DataSync（与既有「无本地事务输入即跳过」
分支同一哲学，见 WAL 包注释）。`runCrashRecovery` 不再返回错误，启动处删除
`logger.Fatal` 包装。

### 修改文件

- `cmd/stratum/main.go`：重试常量（`crashReplayMaxAttempts=3` /
  `crashReplayRetryBaseMillis=100`）与 `replayCoordinatorCall` helper；
  三分支失败改为 skip + 计数；函数不再返回错误；调用处去掉 fatal；注释同步
- `cmd/stratum/main_test.go`：`TestRunCrashRecovery_FailurePropagates` 反转
  为 `_ReplayFailuresSkipped`；新增 KB-not-found 回归与重试语义用例
- `internal/coordinator/mock.go`：`MockWriteCoordinator` 增 `SetReplayFunc`
  （支持「前 N 次失败后成功」的重试断言）
- `internal/wal/wal.go`：记录语义注释同步（不再「requires operator
  intervention」）
- `Stratum_接口设计v9.md` / `Stratum_设计文档v11.md`：崩溃恢复章节表述同步

### 关键设计点

- **永不因重放失败中止启动**：该处失败模式（依赖宕机 / KB 未随 raft 追平 /
  KB 已删除）全部由 Raft 重放 + DataSync 权威恢复；fatal 只会让节点永远等不到
  同步的那一刻，形成死循环。
- 失败记录不主动删除（WAL Abort 语义未做）：保留在 WAL 下次重启重试，计数经
  `GetSystemStatus` 可见；删除路径的 `ErrKnowledgeBaseNotFound` 幂等视为成功
  的既有语义不变。
- 写失败留下的「版本壳子」（元数据已提交、文档未写入）不由此机制删除——由
  客户端收到错误后重试新版本覆盖。

### 验证

- 单测：`go test ./cmd/stratum/ -run TestRunCrashRecovery` 4 用例 PASS
- 全量单测 / race / 进程内集成回归 PASS
- Docker 3 节点事故复现：embed 故障留残 → 节点重启不再 fatal、恢复 healthy；
  T4 docker 全套回归 PASS

---

## 二十三、改动记录：版本删除三模式（设为基底删前置 + 删中间版本）

### 背景

原 `DeleteVersion` 只会「删除目标版本及其全部后代」（状态机里递归
`collectVersionSubtree` 后整棵子树标记 `Deleting`）。对线性版本链而言，这等价于
只能截断链尾，两个实际需求无法满足：

1. 把历史中的某个版本设为新的知识库**基底**（删除它的全部前置版本）；
2. 单独删除一个**中间版本**，而不连带删掉它的子版本。

### 方案

`DeleteVersion` 增加 `mode`（proto 枚举 `VersionDeleteMode`，内部
`types.VersionDeleteMode`）：

| mode | 待删集合 | 结构变更 |
|---|---|---|
| `SUBTREE`（默认，零值） | 目标版本 + 全部后代 | 无（保持历史行为） |
| `SINGLE` | 仅目标版本 | 目标的直接子版本改挂到目标的父版本（链表 splice），分支结构保留 |
| `ANCESTORS` | 目标的全部前置版本 + 这些祖先上挂着的旁支 | 目标的 `parent_version_id` 置 0，成为版本链新的基底 |

待删集合在 Raft 状态机 apply 阶段由 `versionDeleteTargets` 基于当前快照确定性计算
（各副本结论一致）；整个集合**先校验后标记**（含活跃版本 → `ErrVersionIsActive`，
含 PENDING → `ErrVersionPending`，任一违规则整体拒绝），再走既有的
`DeleteVersionCoordinator` 异步幂等清理。响应新增 `deleted_version_ids` 回传被标记
删除的版本清单。

### 修改文件

- `api/proto/knowledgebase.proto`（+ 重新生成的 `api/proto/stratum/knowledgebase*.pb.go`）：
  新增 `VersionDeleteMode` 枚举、`DeleteVersionRequest.mode`、`DeleteVersionResponse.deleted_version_ids`
- `internal/types/types.go`：新增 `VersionDeleteMode` 与 `String()`
- `internal/raft/command.go`：command 增 `Mode`
- `internal/raft/state_machine.go`：`applyResult.DeletedVersionIDs`；
  `applyMarkVersionDeleting` 按 mode 计算/校验/重接；新增
  `versionDeleteTargets` / `collectVersionAncestors` / `reparentChildren` / `spliceParent`
- `internal/raft/raft.go` / `impl.go` / `mock.go`：`ProposeMarkVersionDeleting`
  签名加 `mode` 并返回 `[]int64`
- `service/knowledgebase.go`：`versionDeleteModeFromProto` 映射；
  `DeleteVersion` 传 mode 并回传清单
- `internal/coordinator/delete_version.go`：接口注释同步
- `internal/docstore/{docstore,pebble,mock}.go`：新增
  `DeleteByVersionExceptVisibleFrom`（按可见性回收 MVCC 记录）
- `internal/coordinator/delete_version_impl.go`：Step 4 改为按可见性回收，
  新增 `visibleAnchor`（最小的存活更高版本）；新增 Step 5 调用
  `VersionBloomStore.DeleteByVersion`（配置字段 `VersionBloom` 可选，未接线则跳过）
- `internal/bloom/version_bloom_store.go`：新增 `DeleteByVersion`（清缓存条目 +
  删 `<dir>/bloom-version/<kb>/<version>.bloom`，幂等；两个删除方法都补上
  「未配置持久化目录时 no-op」的防御）
- 测试 double 同步：`internal/coordinator/{write,delete,gc,gc_write_mutex,delete_version}_test.go`、
  `cmd/stratum/main_test.go`
- 新增单测：`internal/raft/state_machine_test.go`（SINGLE splice、SINGLE 边界（目标是
  base / 父版本正在删除）、ANCESTORS 含旁支与新基底、ANCESTORS 重放幂等与 PENDING
  拒绝、断链自愈、非法 mode，以及 mock↔状态机一致性对照测试含 PENDING 幸存者用例）、
  `service/knowledgebase_test.go`（mode 转发、`deleted_version_ids`
  回传、未知 mode 拒绝）
- `web/app.js` / `web/index.html` / `web/style.css`：版本节点「设为基底」按钮；
  删除弹窗改为三模式单选并列出波及版本；`showConfirm` 支持内联附加内容
- 文档：`README.md`、`Stratum_接口设计v9.md`（新增 `DeleteVersion` 章节与 RaftNode 条目）、
  `Stratum_设计文档v12.md`（§1.2.3）

### 关键设计点

- **向后兼容**：`SUBTREE = 0`，旧客户端不传 `mode` 时行为与改动前完全一致。
- **fail-closed**：无法识别的 `mode` 一律返回 `ErrInvalidArgument`，绝不静默降级为
  破坏性最强的 `SUBTREE`（否则一个笔误就会删掉整棵子树）。
- **确定性**：集合计算、约束校验、`parent` 重接全部在 apply 阶段完成，leader 与
  follower 结论一致；复用既有的 `MarkVersionDeleting` 命令，不新增 Raft 命令类型。
- **校验先于变更**：整体校验通过后才执行 splice / 置基底 / 标记，杜绝半删除状态。
- **版本数据零耦合**：每个版本持有独立的 MVCC 文档记录与 HNSW 索引，删除任一版本
  （中间版本或前置版本）都不影响其余版本的查询结果。
- **无悬空父指针**：`SINGLE` 重接时若目标版本的父版本已不存在或正在删除，子版本直接
  成为根（而非挂到一个即将消失的版本上）；`ANCESTORS` 遇到断链也会把目标版本的
  `parent_version_id` 归零自愈。
- **幸存者不得为 PENDING**：`SINGLE` 被重挂的子版本、`ANCESTORS` 中被保留的目标版本，
  若其父版本在待删集合中且自身仍 PENDING，则整体拒绝（`ErrVersionPending`）——它完成
  存储写入（`writeVersionDocList` 读父版本的 `VersionDocList`）与崩溃恢复（按
  `ParentVersionID` 重放）都依赖那个即将被清理的父版本，否则会静默丢失继承来的文档集。
  这是原 SUBTREE 规则（"子树含 PENDING 即拒绝"）在保留后代的模式下失效后的补位。
- **按可见性回收 MVCC 记录**：文档记录是**增量写入**的（每版本只写自己改动的文档），
  读取时按版本回溯到 ≤ 目标版本的最新记录（`docstore.ReadAt`）。因此清理被删版本时，
  只回收**不再被任何存活版本读取**的记录（`DocStore.DeleteByVersionExceptVisibleFrom`，
  锚点 = 最小的存活更高版本）。否则存活版本会静默丢文档或读到回退值——这是
  `SINGLE` / `ANCESTORS` 能保留后代的前提。代价：删除中间/前置版本不会立即释放这些
  文档的空间（它们仍是活跃数据），完全释放发生在删除无存活更高版本的链尾时。
  
  **已知限制**：为存活版本保留下来的记录，在该存活版本随后也被删除时不会立即回收
  （一次清理只处理被删版本自身的记录），会留下不再被任何版本读取的孤儿 MVCC 记录。
  它们不影响查询（每个版本的可检索文档集由自己的索引与 `VersionDocList` 决定），
  但占用磁盘。**该回收尚未实现**：除整库删除外没有自动收敛路径，属于待办。
- **版本自身的数据随删随清**：`IndexManager.Discard` 清内存条目（含墓碑防 Load 复活）、
  vecstore 侧索引与磁盘 `.index`/`.ids`/`.mem`；新增的
  `VersionBloomStore.DeleteByVersion` 清掉该版本的布隆过滤器文件与缓存条目（此前只有
  整库删除才回收，导致每删一个版本就泄漏一个 `.bloom` 文件）。chunk 向量（L2）不随
  版本立即删除，仍由 `ChunkGarbageCollector` 按「最新版本是否引用」周期性回收。
- **幂等**：重复调用安全；`ANCESTORS` 作用于已是基底的版本是空操作，返回空列表。

### 验证

- `go test ./... -timeout 300s -count=1`：全部包 PASS（含 integration）
- `go test -race ./internal/raft/... ./internal/coordinator/... ./service/...`：PASS
- `go vet ./internal/raft/... ./internal/coordinator/... ./service/... ./internal/types/...`：无告警
- `node --check web/app.js`：语法通过

