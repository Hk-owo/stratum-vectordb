# 协调者选择、Leader 发现与存储节点局部健康视图设计

| 项目 | 内容 |
|---|---|
| 文档状态 | 已确认，未实现 |
| 前置阅读 | `storage-coordination-and-service-station-design.md`、`unresolved-issues-resolution-final.md` |
| 本文覆盖 | 控制层 leader 发现、协调者选择、Raft apply 阶段外部副作用的触发时机、写完成后数据位置的查询方式、存储节点间局部健康视图的维护与用途 |

---

## 1. 存储节点向控制层上报（ReportDataDurable / ReportEpoch 等）的 leader 发现

### 1.1 问题
写完成后，负责上报的存储节点/协调者需要把结果发给控制层的 Raft leader；如果上报发起时原 leader 已经切换，调用方不知道新 leader 是谁。

### 1.2 结论：不新增接口，复用现有 `internal/router` 的动态发现+重试模式

代码里已有的 `Router`（`internal/router/router.go` + `leader.go`）解决的正是同一个问题：

```
forwardWrite:
  discoverer.LeaderNow(ctx)   // 轮询所有节点的 GetClusterStatus，取多数节点认可的 leader，不缓存
    → 有结果：直接打这个节点
    → 无结果（选举中/发现失败）：tryAll，挨个节点试一遍
  收到 NotLeader → 重新 LeaderNow 再打一次
  非 NotLeader/Unavailable 的错误 → 直接返回，终态
```

`GetClusterStatus` 本身就是"暴露 leader 信息"，但暴露方式是**每次调用现查、不缓存**——这就避免了"单独给一个静态/缓存的 leader 查询接口"的根本缺陷：查完到实际调用之间还是有窗口，leader 可能又变了，等于把问题往前挪了一步，没解决。

### 1.3 落地方式
将存储节点的上报调用（`ReportDataDurable`/`ReportIndexReady`/迟到重传）归入 `isWriteMethod` 判定集合，走同一套 `forwardWrite` 逻辑；不新增协议、不新增部署单元。若存储节点当前跟控制层走的是独立连接（没有接入 Router），需要先接入，或在存储节点侧照抄一份等价的 discoverer+重试逻辑。

---

## 2. 存储节点"网状但个别节点被感知"的问题：只统一协调者选择，fan-out 保持网状

### 2.1 两处不同性质的问题，不能用同一个方案

- **控制层选协调者**：目前"打到某个存储节点"这句话没细化怎么选——这是该统一的部分。
- **协调者确定后联系其余副本（fan-out）**：这一步不该加代理层，必须保持网状直连。

### 2.2 协调者选择：不建健康检查子系统，靠"试了再说"

不新增控制层自己的健康检查（会与服务站的健康视图独立漂移，两套流量、两套判断标准，故障排查更难）。方案是直接复用 `router.go` 的 `tryAll` 骨架：

```
候选列表 = 该 KB 的副本拓扑（DurabilityPolicy 已有）
候选按稳定顺序排列（如副本ID排序，避免随机顺序导致日志难关联）
for 候选 in 候选列表:
    尝试发起 WriteVersionData
    成功 → 该候选即本次协调者，进入 §1.2 节写入腿流程
    失败/超时 → 换下一个候选
全部候选失败 → 走 1.1 节失败处理路径（记 transient，按重试预算/超时判定）
```

不需要权威健康视图——每次选择都自带验证，跟服务站的熔断目的一致，只是不用为写路径重复建一套。

### 2.3 隐藏的坑：Raft apply 阶段谁能触发外部副作用

"选协调者、发 WriteVersionData"这个触发动作的时机是关键——它不能写在无差别执行的 `Apply()` 里。

**问题**：`Apply()` 在 Raft **每个节点**（leader + 全部 follower）上都会跑一遍，用于保证本地状态机与日志一致。如果谁都能在自己的 `Apply()` 里顺手触发协调者选择+fan-out，一次写入会被 N 个控制层节点各自独立触发一遍——不是正确性错误（内容寻址天然幂等，1.4 节改动2也已处理重复/迟到上报的丢弃校验），但会造成不必要的资源浪费和重复 quorum 链路。

**方案**：外部副作用（协调者选择、fan-out）只能由"应用这条日志时，自己此刻确实是 leader"的节点触发——是 **apply 这一刻现查**，不是"发起 propose 时是不是 leader"。做法与 6.5840 Raft lab 一致：`Apply()` 里正常更新本地状态机数据（每个节点都做，保证状态一致），但对外部 RPC 副作用单独判断一次 `rf.GetState() == Leader` 才发起，follower 的 `Apply()` 只落地状态、不对外发请求。

**接受的窗口**：leader 切换恰好发生在"旧 leader 的 apply 循环还没跑到这条日志"和"新 leader 的 apply 循环也跑到这条日志"之间时，理论上存在极短窗口两边都判断自己是 leader（旧 leader 还没意识到掉线）。靠内容寻址的幂等性吸收，不用分布式锁去堵——堵这个窗口的复杂度收益远不如"允许极小概率重复、靠幂等兜底"。

### 2.4 fan-out 必须保持网状的理由

- **Raft 类比**：leader 复制日志给 follower 从不经过代理——leader 需要精确知道每个 follower 的 matchIndex，代理会模糊这个信号。quorum 确认是同一类问题：协调者需要精确知道"是哪几个副本真的确认了"。
- **服务站职责边界不覆盖这里**：服务站的前提是"发给任意健康副本都能拿到等价结果"，负载均衡/熔断在此前提下才有意义；写的 fan-out 是"这几个确定的副本都必须联系到，一个都不能少"，没有"选哪个都行"的自由度，代理层只会多一跳延迟、多一个故障点。
- **已定机制假设了直连**：1.3 节"副本互相核对状态代协调者上报"、1.4 节 at-least-once 兜底、3.4 节背压熔断（明确是"服务站对存储节点"，不是"协调者对副本"）——都建立在协调者与副本网状直连的前提上，插一层代理需要重新审查这些机制的正确性前提，没有收益却要付出审查成本。

### 2.5 完整流程

```
Raft 日志提交
  → 每个控制层节点 Apply()：更新本地版本元数据（所有节点都做）
  → 仅当前节点 apply 这一刻确实是 leader：
       候选列表 = 该 KB 的副本拓扑
       按顺序 try 候选直到成功 → 确定协调者
       控制层不再插手，协调者直接网状 fan-out 给其余副本（既有写入腿流程照常）
```

---

## 3. 写完成后"数据最终在哪"的查询

### 3.1 结论：不记快照，复用 `ReportEpoch` 聚合，不新增机制

不该在写完成那一刻记一份"确认副本清单"——这份清单从记下来那一刻起就开始过期（副本可能被换掉、可能落后需要补链、也可能后来才追上）。

该用已有的 `ReportEpoch`（2.4/2.5节）：每个存储节点周期性上报自己每个 KB 的 `localVersion`。查询"版本 V 现在活在哪几个节点"时，只需要问：**当前哪些节点最近一次上报的 `dataVersions[kb] >= V`**——这是持续刷新的聚合结果，不是一次性快照，节点掉队/追上都会自动反映。

### 3.2 用途
- **有针对性地修复**：某副本永久失效需要换新节点补数据时，从"确认持有该版本"的节点里挑一个当数据源。
- **指导落后节点该找谁补链**：2.1 节"落后节点向 peer 请求区间补齐"，该问哪个 peer 也来自这份信息。
- 与 1.4 节清理广播不冲突——清理广播要"宁可错杀不可放过"，照 `DurabilityPolicy` 候选集合全量广播，不依赖这份信息，两者用途不同。

### 3.3 新鲜度权衡
`ReportEpoch` 上报周期决定了这份信息的滞后程度——属于正常的新鲜度权衡（与一致性窗口占位值同一性质），不是缺陷，不需要单独再定一套。

---

## 4. `ReportEpoch` 聚合信息的维护位置

### 4.1 结论：leader 本地内存，不进 Raft、不持久化

不应作为 Raft propose 写入日志——`ReportEpoch` 是高频、软状态，套上强一致复制成本过重；这份信息本身允许几秒滞后（同一致性窗口占位值的权衡），不需要线性化保证。真正需要 Raft 强一致的是"数据/索引 READY 状态"（直接决定查询可见性）——这条边界不能与"数据在哪"这种运维参考信息混同。

维护在**当前 leader 节点的内存里**：`nodeID → kbID → dataVersions` 的简单 map，`ReportEpoch` 到达时直接更新，不经 Raft propose。

### 4.2 存储节点如何找到该发给谁
与 §1 同一个问题——`ReportEpoch` 归入"必须打到当前 leader"的写方法集合，复用 Router 的动态发现+重试逻辑，不单独设计。

### 4.3 leader 切换时的行为
新 leader 上任时这份内存 map 为空，不做任何"迁移旧 leader 状态"的动作——存储节点下一轮 `ReportEpoch`（走 NotLeader 重试）自然会打到新 leader，一个上报周期内信息自己补齐。与 3.4 节熔断状态（本地、不同步）同一类设计：**软状态允许在故障切换时清零重建，追求持久化是过度设计**，用途（运维修复、指导补链）完全能接受这个代价。

### 4.4 两层状态的边界
- **Raft 复制的强一致状态**：版本元数据、Saga 完成状态——决定"能不能被查到"。
- **leader 本地软状态**：健康检查、熔断、`ReportEpoch` 聚合——辅助"运维决策该找谁"。
  两者不该用同一套持久化标准。

---

## 5. 存储节点之间的局部健康视图

### 5.1 不该有一个权威维护者

不选一个专门节点或搞小型选举来维护"哪个存储节点健康"这份信息——这会凭空造出一个新的共识问题（维护者本身需要被选出、崩溃后需要重选），而健康状态天然不需要共识：每个观察者看到略有出入的版本完全可以接受。方案是**每个存储节点各自维护自己对其他节点的局部认知**，互不对齐、互不共享。

### 5.2 怎么维护：顺着真实流量捎带，不建独立探活子系统

- 协调者 fan-out、副本互相核对代为上报（1.3节）——这些真实调用的成败，节点顺手更新自己"跟这个节点最近一次通信成不成功"的记录，不单开心跳/ping 子系统。
- 对长期没有真实流量往来的节点对（如某 KB 很久没写），补一个轻量、低频、尽力而为的心跳兜底，只刷新粗粒度的"最近还活着"信号，不追求精确。
- 这份视图**只用于优化，不用于正确性判断**：协调者选择时可以优先跳过"最近观察到连不上"的候选，减少无谓超时；判断错了也没关系（该节点其实已恢复），因为最终仍靠"试了再说"的 fan-out/quorum 兜底——局部视图只是让"试"少走弯路，不是让它可以被跳过。

### 5.3 崩溃层面为什么天然没问题
没有维护者，就没有"维护者崩溃"这回事——某节点崩了，丢的只是它自己对别人的局部认知，不影响其他节点各自维护的那份；重启后从空白开始，靠后续真实流量重新捎带建立，不需要恢复或选新维护者。与服务站熔断状态（3.4节）、`ReportEpoch` 聚合（§4）是同一套哲学：**局部、软状态、允许过期、靠实际尝试兜底正确性**。

### 5.4 这份数据的用途：只在本节点内部消化，不出节点边界

1. **协调者选择时排候选顺序**（§2.2）：当前节点被选中当协调者时，照局部认知把"最近能通"的候选排前面。只是排序优化，不是准入门槛——排错了也不影响 quorum 凑齐，只是多等几次超时。
2. **存储节点之间复用 3.4 节的背压/熔断骨架**：协调者若最近连续几次跟某副本通信失败，可临时降低该副本的尝试优先级或短暂跳过，冷却期后再放量试探——与 3.4 节服务站对存储节点的熔断同一套三态模型，直接复用，不重新设计。

**没有第三个用途**——不进 `ReportEpoch`，不上报控制层，不给服务站共享（服务站有自己独立的健康检查，各查各的：两边探测频率、超时阈值不对齐会导致视图分歧，共享反而让故障排查更难）。这份数据出了本节点的进程边界就不再有意义。

---

## 6. 索引构建失败产物的回收（自超时兜底）

### 6.1 缺口

§2.2 的候选选择逻辑是"按顺序试候选，失败就换下一个"。用在索引构建（`EnsureIndex`）上：候选 A 被打中、本地状态切到 `BUILDING`、构建到一半崩溃或超时——控制层直接换下一个候选 B，B 后来成功，整个 version 索引最终 READY。**这个过程里 version 从头到尾都没进入过 `FAILED_PERMANENT`**，§1.4 节（`unresolved-issues-resolution-final.md`）的清理广播不会触发。候选 A 磁盘上那份没建完的半成品索引，此后没有任何机制会去碰它——这是比"整个 version 判死"频繁得多的日常情况（普通的瞬时故障/超时重试），是这条缺口的具体所指。

### 6.2 不采用的方案：控制层记录"曾在哪些候选上尝试过"

会重蹈本文档反复拒绝过的模式（§3.1 记快照、§5.1 建权威健康表）——这份记录本身会过期（候选节点后来重启、磁盘更换等），维护它是在给一个软状态的东西强上一致性 bookkeeping，收益不成比例。

### 6.3 方案：候选节点凭本地文件状态自愈，不依赖外部通知到达

**关键实现要求：判断依据必须是磁盘上产物文件的 mtime，不能是进程内的计时器**——如果候选节点自身也崩溃重启，内存里的计时器会跟着消失，之后无法感知"这份半成品已经放了多久"；用文件 mtime 判断则天然在进程重启后依然成立。

```
后台定期扫描（周期性任务，与该节点上的构建活动解耦）：
  遍历本地磁盘上处于 BUILDING 状态的索引产物（未被 Save 封印，即无 READY 标记/sidecar）
  for 每份 BUILDING 产物 (kbID, versionID):
      若 now - 产物文件 mtime > BuildAbandonTimeout:
          直接删除该半成品产物文件
          本地状态复位为"无此 version 的构建记录"（如同从未尝试过）
```

`BuildAbandonTimeout` 占位值：**30 分钟**——依据是已知性能目标"10万 chunk 索引构建 5 分钟内完成"，留 6 倍余量，避免跟合法的慢构建（大批量、冷缓存）撞车；实测数据出来后按同一套"占位值集中放配置结构体、待压测替换"的原则调整，不单独定一套标准。

**为什么这条不会误杀真正在推进的构建**：只要构建过程中持续调用 `AddChunks`/`RemoveChunks` 等操作，产物文件的 mtime 会跟着刷新，扫描时算出的"距上次活动时间"就不会超阈值；只有真正卡死不再产生任何磁盘写入的半成品才会被清理。

**为什么不需要外部信号送达**：这份自愈完全基于节点自己观察"太久没有新动作"，不依赖控制层显式通知"你被放弃了"——这条通知本身可能因为控制层已经换了候选而根本不会发、或者传递中丢失。跟 §5.2 局部健康视图、§4.3 leader 本地软状态清零重建是同一套哲学：**不确定时保守地自己收拾，不指望一个必达的外部通知**。

**与既有 `Build`/`Reset` 行为的关系**：若该候选节点未来还会为同一个 version 收到一次新的合法构建请求（比如它后来又被选中重试），既有的"`Build` 自身会 `Reset` 覆盖"行为会自然覆盖掉旧的半成品，不需要专门清理——本节的自超时兜底，只覆盖"这个节点未来永远不会再为这个 version 收到构建请求"这一段没人管的空隙（version 最终在别的候选上成功，或整个 version 判死）。

### 6.4 `unresolved-issues-resolution-final.md` §1.4 清理广播需扩展覆盖范围

现有 §1.4 的清理广播（`FAILED_PERMANENT` 时对全部候选副本广播 `DeleteByVersion`）只覆盖了 docstore 数据，需要显式扩展到索引产物：广播内容里加一项，要求每个候选节点收到后，除了清理 docstore 数据，也检查并清掉自己本地任何跟该 version 相关的索引构建残留（不论当前是 `BUILDING` 还是其他残留状态），不必等 §6.3 的自超时窗口过期。两者不冲突——§1.4 广播覆盖"整个 version 判死"这种终态、可以立即清理的情况；§6.3 自超时覆盖"version 在别处构建成功、这个候选被静默放弃"这种没有终态信号、只能靠自己观察的情况。