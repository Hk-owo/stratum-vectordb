# 存储层协调协议 / 索引构建加速 / 读路径服务站 —— 补充设计

| 项目 | 内容 |
|---|---|
| 文档状态 | 草案（Design Draft），未实现 |
| 前置阅读 | `control-data-separation-design.md`（v1，本文档是它的补充与修订） |
| 本文性质 | 对 v1 草案中若干悬空/待决问题的具体化设计。文中标注 `【修订】` 的部分是对 v1 原文的修改，`【新增】` 的部分是 v1 未覆盖的新内容 |

---

## 0. 本文档解决 v1 的哪些待决事项

| v1 待决事项 | 状态 |
|---|---|
| #1 存储集群内部用什么协调？ | 本文第 2 章给出具体协议 |
| #3 读路径是否完全绕开控制层？ | 【修订】v1 原答案"是"被推翻，见第 4 章 |
| #4 一致性窗口的量级约束 | 部分回应：窗口的**结构**已定（见 2.5 节），具体数值仍需实测，未闭合 |
| #6 索引是否参与跨节点复制？ | 【修订】v1 原答案"否"被部分推翻，见第 3.2 节 |
| 5.1 节 Saga 永久失败终态 | **仍未解决**，见第 5 章 |
| 5.3 节 epoch 恢复协议的 payload | 【修订】见第 2.5 节 |

---

## 1. 写路径：数据 quorum 与协调者协议

### 1.1 quorum 判定标准

存储层内部以**多数派（quorum）确认**作为"durable"的判定标准，通过 `DurabilityPolicy` 表达（Replicas=N 时，quorum = ⌈(N+1)/2⌉）；EC 场景对应为"k 个可解码分片"。

三态对齐：
- ≥ quorum 确认 durable → `AVAILABLE`
- 有副本但不足 quorum → `DEGRADED`
- 零副本 → `UNAVAILABLE`

### 1.2 协调者：临时角色，不是 leader

`DataPlane.WriteVersionData` 打到的**那一个**存储节点，临时充当这次写入的协调者：

1. 协调者把 `changes` fan-out 给该 version 的全部 N 个目标副本（含自己），**同时把"目标副本列表 + quorum 阈值"一并发给每个副本**（4.1 节 `DataPlane` 接口需要为此新增字段——现状只发数据，不发副本拓扑）。
2. 各副本落盘后各自确认。
3. 协调者数到 quorum 个确认，调用 `ReportDataDurable`。

协调者角色随请求产生、随请求结束，不跨请求持久化，不需要选举——这与 leader 的本质区别在于：每个 version 的 changes 内容在 Raft commit 时已经确定、不需要跨 version 维护顺序，所以不需要一个长期占据的协调者来维持全局日志顺序。

### 1.3 协调者崩溃后的接管

**问题**：协调者收齐 quorum 确认后、上报 `ReportDataDurable` 前崩溃——数据本身没丢（quorum 个副本都有），但"已达 quorum"这个事实只存在于协调者内存里，随之消失。

**机制**：收到写入的每个副本节点设一个短暂本地超时（毫秒级，副本间直连、局域网延迟）；超时内未收到协调者的"quorum 已达成"收尾信号，副本节点互相核对各自的确认状态，任意一个副本数出已达 quorum 后，代替协调者调用 `ReportDataDurable`。

这套"接管"顶替的不是数据传输（副本已有数据，无需重发），只是"发现 quorum 已凑够、并负责上报"这最后一步。

### 1.4 客户端 at-least-once 保留，作为最终兜底

若协调者所在节点整体丢失（连磁盘都没了），1.3 节的副本互相接管仍能工作（数据在其余副本上）；但若**这一批**（协调者+其 fan-out 到的全部目标节点）同时丢失，只能靠调用方（网关/SDK）保留原始数据、在未收到 quorum 确认前不得丢弃，视为 at-least-once 语义。得益于 chunk 内容寻址，重试是幂等的（已收到的 chunk 直接跳过），代价很低。

需要把这一条显式写进 5.1 节 Saga 描述的前提条件里（v1 原文隐含了这个假设，但从未言明）。

---

## 2. 存储层内部协调协议（对应 v1 待决 #1）

### 2.1 数据游标：严格线性，标量表示

版本链是**严格线性**的（无 rollback 分叉）。每个存储节点本地维护 `localVersion map[kbID]int64`，表示该 KB 已完整应用到的 version 号。

**强制不变式**：节点收到 version V 的 `changes` 时，若 `localVersion[kb] < V-1`，不得直接应用——必须先向任意一个 `localVersion[kb] >= V-1` 的 peer 请求区间 `(localVersion[kb], V-1]` 的变更补齐，按序应用后才应用 V。此规则堵住断链风险，也是"版本号比较即可代表完整性"这一简化成立的前提。

### 2.2 节点间同步

节点间定期（或写入 fan-out 时顺带）交换 `localVersion[kb]`；落后方按 2.1 节规则主动追平。因为是标量整数比较，不存在"半新不旧"的中间损坏态，不需要像 chunk 级 gossip 那样处理误传播窗口问题。

### 2.3 控制层调度：changes 内容与应用顺序解耦

changes 在 Raft commit 时内容已经确定（不依赖存储层此刻进度），因此控制层可以**并发**向多个协调者发起多个 version 的 `WriteVersionData`，不需要为了保证顺序而串行等待——顺序的执行由 2.1 节的不变式在存储节点侧保证。

**有界并发窗口**：需要对同一 KB 设置"允许同时在飞的未完成 Saga 数量"上限，防止某个 version 卡住导致后续无限堆积在存储节点的暂存区。达到上限后新写入排队；卡住的 version 应转入第 5 章要补的"永久失败"终态处理，而不是无限等待。

### 2.4 恢复时的临时协调者

任意存活节点重启后，向其余节点询问 `localVersion[kb]`，凑够 quorum 个报告里的**最小值**，即为可安全上报给控制层的 durable version。

### 2.5 ReportEpoch payload【修订】

v1 原始设计：`ReportEpoch(epoch, durableVersions []VersionRef)`。

修订为：

```go
ReportEpoch(epoch uint64,
    dataVersions map[kbID]int64,          // 数据游标，标量，线性链保证完整性
    indexReadyVersions map[kbID][]int64,  // 索引就绪的 version 集合，见 3.1 节，为何不能是标量
)
```

数据侧压缩成标量游标是合法的（线性链的完整性由顺序应用保证）；索引侧不能这么做，见第 3 章。

---

## 3. 索引构建：与数据侧完全不同的性质【新增，对应 v1 待决 #6 的部分修订】

### 3.1 索引游标不能是标量

数据必须按链严格顺序应用，一个整数游标天然蕴含"之前全部完整"。索引构建**没有这条约束**——v12 设计文档明确"每个版本的量化器独立...不存在跨版本共享码本"，version V+1 的索引可能先于 V 建好（V 卡在重试、V+1 后来居上）。因此索引侧的状态必须是 `map[int64]bool`（每个 version 各自的 READY/未 READY），且只需覆盖 `IndexRetentionCount` 保留窗口内的 version——窗口外的版本按需走 `RebuildIndex`。

### 3.2 索引 quorum 的含义与数据 quorum完全不同【修订 v1 待决 #6】

v1 待决 #6 原本建议"索引不参与跨节点复制，各节点从原始数据本地重算"。这个结论建立在"节点间不能直连"的前提上；网状拓扑确立后需要重新论证：

- **索引从不需要持久性保护**——它是派生物，L2 层（RocksDB 全精度向量，受数据 quorum 保护）不丢，索引随时可重算，不存在不可逆损失。
- 索引侧的"quorum"（多少个节点 READY 才算不 DEGRADED）保护的是**读路径的服务能力分布**，不是持久性——是一个负载/容量问题。DEGRADED 的准确含义应改为"当前能扛读流量的节点数低于预期，读请求有较大概率打到需要现场触发构建的节点、延迟劣化"，而不是"有丢失风险"。
- 索引服务能力阈值应该是一个**独立于** `DurabilityPolicy.Replicas` 的配置项，二者维度不同，不该共用一个数字。该阈值由控制层声明式配置还是存储层动态决定，**待定**（见第 5 章）。

### 3.3 构建也走"建一次、分发 N 份"，复用写路径的协调者模式

同一个 version 的索引没有必要在 N 个副本节点各自重复训练+构建（重复计算、且并发构建可能因插入顺序不同产生略有差异的码本）。改为：

1. `EnsureIndex` 打到的存储节点临时充当"构建协调者"，实际执行训练+构建+Save。
2. 建好后把索引文件（含码本/量化状态，随 `write_index` 一并序列化）分发给其余副本，副本 `Load` 即可，不重新构建。
3. 副本确认 Load 成功，凑够索引服务能力阈值后，协调者上报 `ReportIndexReady`。

**前置依赖，必须先补**：现状索引写入非原子（无 `.tmp+rename`）、无 checksum（`Load` 不校验 `ids.size()==ntotal`）。本地磁盘 corruption 尚可接受（重建即可，损失可控）；一旦引入跨网络分发，网络传输本身可能损坏文件，且现有 `Load` 无法感知——**必须在做文件分发之前先补 checksum 校验**，否则会引入一个比本地磁盘偶发 corruption 更容易触发、且是静默的正确性风险。

### 3.4 崩溃接管：不预留 standby，动态指派

**结论**：不为每个构建任务固定绑定 standby 节点，6 个节点可同时开 6 个独立构建任务，最大化正常态并行度。

理由：
- 构建失败的代价只是"这个 version 多在 DEGRADED 里待一会儿"，不是数据风险（3.2 节已确认索引可随时重算）。
- 预留 standby 换来的收益（缩短故障后的调度延迟）只在低概率的崩溃时刻兑现；不预留换来的收益（吞吐翻倍）持续存在。
- standby 节点即使预留，也没有断点续传能力（构建中间态未落盘），一样要从头重建——预留省下的只是"发现崩溃到指派接手者"这一小段调度延迟，不是重建本身的耗时。

**例外**：若未来业务提出关于"version 多久内必须可查询"的硬性 SLA，需要重新评估这个取舍（当前没有这样的约束）。

### 3.5 构建成本的三层优化（按效果从大到小排序）

**(a) 免图分层【新增，与"冷热分层"（v1 §6.2）结合】**

图边开销（约 0.2-0.6 GB/百万点，与量化无关）在量化后成为内存主导项；构建耗时的大头也在"图插入"这一步（O(n log n) 带较大常数）。冷版本（非活跃、长时间无查询）不值得建图：直接使用 Faiss 的免图量化索引（`IndexPQ`/`IndexScalarQuantizer`，暴力扫描量化码，PQ 的 ADC 可查表+SIMD 加速），构建耗时因省掉图插入而大幅下降；查询退化为 O(n) 扫描，n 不大时完全可接受，且量化损失同样有 rerank 兜底，不引入新的精度风险。热版本仍正常建图。冷热转换时按需重建对应形态。

**(b) 按需触发（惰性构建）【新增】**

现状（v1 5.1 节⑤）是每个被创建的 version 无条件触发 `EnsureIndex`。改为只在 version 成为 `ActiveVersionID` 或被显式访问（历史查询/rollback）时才触发构建——配合写入攒批（见 3.6 节），能过滤掉大量"创建后很快被取代、从未被查询"的中间 version 的构建开销。

需要拆分 Saga 完成判定：**数据 READY**（quorum 个副本 durable，可随时按需构建索引）与**索引 READY**（真正建完）分离。非活跃 version 只需数据 READY 即可视为 Saga 完成，不必等索引；活跃 version 需要等到索引 READY。

首次查询延迟的量级（未构建索引、临时暴力扫描 n 个 chunk 的代价）：

| n | 计算成本 | 磁盘读成本（主导项，取决于缓存命中） |
|---|---|---|
| 1 万 | < 1ms | 热缓存几十 ms；冷缓存 SSD 几十到几百 ms |
| 10 万 | 个位数 ms | 热缓存百 ms 级；冷缓存 SSD 秒级 |
| 100 万 | 十几 ms | 热缓存秒级；冷缓存 SSD 十几秒到 1 分钟 |

（以上为数量级推理，非实测数字，需按 v12 文档附录 D 的方法用真实 RocksDB 配置复现。）n 到十万级以上时，暴力扫描兜底的意义已经不大（跟等免图索引建完是同一量级），此时应直接短暂阻塞/拒绝查询，不做暴力扫描。n 较小时，可以"首次查询暴力扫描返回结果，同时异步后台补建索引"作为过渡。

**(c) 增量复用（仅适用于纯追加场景）【新增】**

Faiss API 核实结论（基于官方文档与社区 issue，非本仓库 vendored 1.9.0 源码实测，建议实现阶段用最小用例复核）：

- `IndexHNSW::add` 原生支持对已存在的图追加插入，与现状"分批 AddChunks"机制一致——**纯追加场景**可以以 version N-1 的图为起点，只对 delta 新增的 chunk 做增量 `add`，无需全量重建。
- `IndexHNSW` **不支持 `remove_ids`**（官方文档只列出 `reset()`；Faiss 官方 issue 区有直接对应的问题"HNSW not support remove_ids?"确认这一点）——HNSW 图结构里节点被其他节点的邻居表引用，删除需要修复图连通性，Faiss 未实现这个操作。
- **含删除的场景**：不能走真正的增量删除，退化为"墓碑过滤 + 延迟全量重建"——用类似现有版本/KB 删除墓碑的模式，在查询结果里过滤已删除的 chunk_id，墓碑占比累积到阈值后触发一次全量重建。触发阈值待实现阶段结合真实删除频率标定。
- 量化码本失配：需要训练的类型（SQ8/PQ）增量场景下新增 chunk 可能使旧码本略有偏移，免训练类型（SQ_BF16/SQ_FP16）无此问题；可接受码本轻微失配，或定期（隔几个 version）重新训练刷新。

### 3.6 写入攻批（debounce），降低 version 产生频率【新增】

现状每次写入调用都各自触发一整套流程（Raft 共识往返、数据 quorum 确认、控制层状态机记录、`ReportEpoch` payload 增长、chunk 去重检查）——这些成本按"写入调用次数"计费，而不是按"实际语义变更量"计费。

引入短暂攒批窗口（毫秒到秒级，或按累积 changes 大小触发，两者取先到者），窗口内到达的多次写入合并为一次 `ProposeCreateVersion`。窗口越大，version 产生频率降得越多，但写入的可见延迟相应增加——具体窗口大小取决于业务对"写入后多久必须可查"的要求，**目前没有具体数字，待业务确认**。

---

## 4. 读路径：服务站【修订，推翻 v1 §5.2"控制层不参与读路径"】

### 4.1 为什么要推翻原设计

原设计的读路径风险：
1. **返回过时结果而不自知**——某节点因分区/暂时落后，`localVersion[kb]` 落后于当前活跃版本，但本地数据完整、会正常应答查询，网关无法感知返回的是旧版本结果，直接损害 MVCC 版本语义最看重的"as-of"精确性。
2. **鉴权/多租户隔离缺失**——`DataPlane.Search` 接口未涉及调用方权限校验，读路径绕开控制层后，任何能直连存储节点的调用方即可查询任意 kbID。
3. **恢复窗口期间读的语义未定义**——节点重启后到 `ReportEpoch` 之间的窗口，本地状态尚未完成自我校验，缺少显式的"暂不可信"标记。

### 4.2 服务站定位

独立于控制集群之外的一层（对应 v1 拓扑图中一直存在但从未展开设计的"网关/路由"）：**无状态、可水平扩展、查询结果经过它中转**。

之所以数据经过它不违反"控制层轻量"的前提——因为它根本不是控制集群的一部分，不需要参与 Raft 共识，可以像标准无状态反向代理（Nginx/Envoy 模式）一样线性扩容应对 QPS 和带宽，不存在方案甲（把数据代理塞进控制集群）当初的天花板问题。

### 4.3 职责

1. **路由表缓存**：本地缓存"KB+version → 可服务节点列表"，从控制层（通过类似 `ReportEpoch` 的机制）异步刷新，不是每次查询都问控制层。
2. **新鲜度凭证校验**：转发查询前附上"当前应看到的版本号/epoch"，存储节点核对本地 `localVersion[kb]` 是否达标，不够则拒绝——把 4.1 节风险 1 从"悄悄发生"变成可检测、可拒绝的显式条件。
3. **负载均衡**：一个 version 有多个 READY 副本时，决定发给哪一个。
4. **故障转移**：某节点因凭证不达标或不可达而拒绝，服务站在同一客户端连接内部换下一个候选节点重试，客户端无感。
5. **鉴权**：租户/权限校验收敛到这一个关卡，存储集群可以完全不对外暴露，只有服务站能访问。
6. **健康检查/熔断**：探活存储节点，路由表标记不健康节点。

### 4.4 待细化的工程点（已确认要做，细节未定）

- **连接管理**：维护到存储集群全部节点的连接池，不逐次新建连接。
- **超时预算传播**：客户端超时预算需要在"服务站内部重试链条"（连节点 A 被拒→换节点 B）内分摊，需定重试次数上限。
- **延迟预算重新核算**：调用链变为 客户端→服务站→存储节点（两段式检索）→服务站→客户端，比直连多一跳，需要与 v12 附录 D 已有的延迟实测数据一起重新核算总延迟。
- **背压/熔断**：慢节点（如触发现场构建）对服务站连接池资源的影响，需要退避策略。

---

## 5. 仍未解决的事项【待续】

1. **Saga 永久失败终态**（v1 待决 #2）：`EnsureIndex`/`WriteVersionData` 持续失败时，可用性模型缺一个"需人工介入"的终态，尚未讨论判断标准该由控制层（重试次数/超时，逻辑层面）还是存储层（失败原因，物理层面）决定。
2. **索引服务能力阈值的归属**（3.2 节遗留）：控制层声明式下发，还是存储层根据观测到的读流量动态决定，未定。
3. **一致性窗口的量级约束**（v1 待决 #4）：多处协议（quorum 确认延迟、epoch 恢复窗口、写入攒批窗口、首次查询暴力扫描的可接受 n 值）都依赖"业务能接受的具体数字"，目前全部悬空，需要业务侧给出明确的 SLA 才能标定。
4. **4.4 节四个工程细节**：已确认要做，具体方案未展开。
