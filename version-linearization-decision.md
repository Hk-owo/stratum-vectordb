# 版本树线性化决策

| 项目 | 内容 |
|---|---|
| 决策 | 丢弃版本树分叉能力，`CreateVersion` 强制父版本最多一个子版本 |
| 触发原因 | `docstore.ReadAt` 按版本号数值大小做祖先关系的替代判断，在分叉场景下会读到兄弟分支的数据（详见下方"问题根源"），且现有测试未覆盖 |
| 影响范围 | 当前实现（`stratum-vectordb` 仓库）+ 与其保持一致的 `storage-coordination-and-service-station-design.md` 2.1 节 |

---

## 1. 决策

版本链改为**严格线性**：一个父版本最多允许一个子版本。`internal/raft/state_machine.go` 的 `CreateVersion` apply 阶段，在现有父版本校验（存在 / 同 KB / 非 PENDING / 非 Deleting）之上，新增一条：**父版本已存在子版本时拒绝**。

这条决策让 `storage-coordination-and-service-station-design.md` 2.1 节"版本链严格线性、无 rollback 分叉"这条假设，从"跟当前实现不符的草案假设"变成"当前实现该收敛过去的方向"——两份文档的地基重新统一，不再需要跨接分歧。

## 2. 问题根源（决策依据）

`docstore.ReadAt(kbID, docID, maxVersionID)`：取该 docID 所有条目里"版本号 ≤ maxVersionID 的最大一条"，是纯数值比较，不判断祖先关系。这个替代关系只在版本链是线性（全序）时才等价于"沿祖先链能读到的最新版本"；一旦允许分叉，数值序（全局单调递增）和祖先序（树上偏序）不再等价。

复现路径：公共祖先版本 5 分叉出兄弟分支 B、C；C 在版本 15 独立改写文档 D；B 从未再碰 D，推进到版本 20；查询"B@20"的 D 时，`ReadAt` 全局扫描 D 的条目，命中版本 15（C 写的，不在 B 的祖先链上），而不是版本 5（B 血缘链上真正该读到的祖先内容）。

`versionDocList.ListDocIDs` 这一层（判断 D 是否属于版本 20 的文档集合）是按 `ParentVersionID` 真祖先链算的，是对的——问题只出在"确认 D 在集合里之后，取 D 的内容"这一步，membership 检查和内容读取用了两套不同的"谁在先"标准。现有测试 `TestIntegration_ForkedVersions` 两个分支写的是不同 docID（`doc-a`/`doc-b`），没测到"同一 docID 在两个分支各自独立改写"这个场景，所以一直没被抓到。

线性化之后这个问题不需要专门修——版本号数值顺序和祖先关系重新画等号，前提不成立了。

## 3. 连带影响，需要一并处理

1. **README 卖点调整**：原文"A/B testing — multiple versions can coexist and be queried in parallel"依赖分叉（两个候选版本从同一父版本同时派生、并行比较），线性化后这个具体场景不成立，需要拿掉或改写。"查询任意历史版本做审计/对比"这条不受影响，继续成立。
2. **`DeleteVersion` 三种模式语义退化，需要重写注释/文档**（代码逻辑本身不必推翻，线性链下天然收敛）：
   - `SUBTREE`（目标 + 全部后代）→ 退化为"删除该版本及其之后的全部"，即删尾巴，不再是树意义上的子树。
   - `SINGLE`（仅目标自身，子版本改挂父版本）→ 线性链下最多一个子版本要重挂，本质就是链表删除中间节点，逻辑不变但注释里"子版本"（复数）要改成"唯一后继"。
   - `ANCESTORS`（目标全部祖先 + 兄弟分支）→ 退化为"删除该版本及其之前的全部"，即删前缀，"兄弟分支一并删除"这句要去掉（线性链没有兄弟分支）。
3. **产品定位不受影响**：此前定的"可追溯到任意历史版本，不做破坏性分叉"这条本来就是照着线性化之后的语义定的，不需要跟着改。

## 4. TDD 下一步（尚未执行）

1. 先写失败测试：父版本已有子版本时 `CreateVersion` 必须返回错误（对应 `state_machine.go` 校验逻辑新增点）。
2. 改写/删除 `TestIntegration_ForkedVersions` 里验证"分叉成功"的断言。
3. 校验逻辑补齐后重跑全量测试确认无回归。

---

## 5. 附：删除中间版本是否允许

**允许，而且现有机制在线性化之后基本已经是正确的**，不需要新设计一套删除逻辑：

- **元数据层**：`SINGLE` 模式本来就是"删除目标自身、把它的子版本重新接到目标的父版本"——线性链下这就是标准的链表删除中间节点，直接复用，不需要改结构，只改注释（见第 3 节第 2 点）。
- **MVCC 数据回收层**：`DeleteByVersionExceptVisibleFrom` 用的 anchor 语义（"大于 versionID 的最小幸存版本"）在线性化之后不再有歧义——因为版本号数值顺序等价于祖先顺序，anchor 找到的一定是真正的直系后继，不会像分叉场景那样可能是不相关的兄弟分支。也就是说，之前担心"anchor 回收逻辑复用了同一个有问题的替代关系"这条顾虑，线性化之后自动解除，不需要额外修复。
- **幸存者保护**（`validateSurvivorsNotPending`）：线性链下"选中删除集合、但父在集合里的版本"最多只有一个（唯一后继），逻辑不用变，只是判断对象从"多个子版本"收窄成"至多一个后继"。

**唯一真正悬空、需要额外设计的一环**：如果被删除的中间版本，其原始变更数据（不是 docstore 里保留的 MVCC 内容，是"这个版本相对上一个版本改了什么"这份增量记录）还会被其他机制依赖——具体是 `storage-coordination-and-service-station-design.md` 2.1 节那条"节点落后时向 peer 请求区间 `(localVersion, V-1]` 的变更补齐"的追链机制。如果这个区间正好盖住一个已经被删除、只保留了 MVCC 快照内容、没保留原始增量记录的中间版本，追链就补不出来。

这条在更早一轮已经提出过方案：**追链请求命中已删除区间时，退化为全量状态传输**（类似 Raft InstallSnapshot），落后节点直接把 `localVersion` 跳到全量传输对应的版本号，不重放中间那些已经不存在的增量。这个方案现在因为"删除中间版本"被确认是要支持的常规能力（不只是删尾巴），从"未来锦上添花"变成了"分布式多节点场景下必须要有的兜底"，需要在真正做那套多节点存储协调时一并实现，不能再往后拖。
