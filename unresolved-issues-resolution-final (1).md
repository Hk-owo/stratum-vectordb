# 存储层协调协议 —— 遗留事项收敛（定稿 v2）

| 项目 | 内容 |
|---|---|
| 文档状态 | 已确认，未实现 |
| 前置阅读 | `storage-coordination-and-service-station-design.md`（本文档收敛其第 5 章遗留事项） |

---

## 0. 收敛状态总览

| 上一版遗留事项 | 状态 |
|---|---|
| 1. Saga 永久失败终态 | 【已定】见第 1 章 |
| 2. 索引服务能力阈值归属 | 【已定】见第 2 章 |
| 3. 一致性窗口量级约束 | 【已定，占位值】见第 4 章 —— 待压测替换 |
| 4. 4.4 节四个工程细节 | 【已定】见第 3 章 |
| 5.（新增）写入攒批的隐含风险 | 【已定】见第 4.1 节 |
| 6.（新增）判死后的物理数据清理与迟到上报 | 【已定】见第 1.4 节 |

---

## 1. Saga 永久失败终态

### 1.1 判定权：控制层兜底，存储层原因码加速

控制层拥有终态判定权（重试次数 + 超时）。存储层每次失败上报时附一个原因分类：

```go
type FailureReason int
const (
    FailureTransient   FailureReason = iota // 网络/节点重启/磁盘满——换节点大概率成功
    FailureFatalGlobal                       // 数据全局不可用/KB已删除/配额拒绝——重试无意义
)
```

- `FailureFatalGlobal`：不等重试预算耗尽，直接短路进终态。
- `FailureTransient`：计入重试预算，按次数/超时逻辑处理。

### 1.2 终态定义
新增 `FAILED_PERMANENT` 状态，数据侧和索引侧各自独立拥有。

### 1.3 人工介入的最小闭环
`FAILED_PERMANENT` 时必须记录失败原因链。预留但暂不实现：`ListFailedVersions(kbID)`、`ForceRetryVersion`/`ForceAbandonVersion`。

### 1.4 终态触发清理广播 + 迟到上报的丢弃校验【新增】

**背景**：控制层判定写入失败进入 `FAILED_PERMANENT` 时，物理数据可能已经真的写成功（比如某个副本节点实际落盘了，但确认消息丢失、超时导致控制层以为失败）。这份数据不会被任何查询路径读到（可见性由控制层状态驱动，不取决于数据是否physically存在），但也没人会去回收它——需要补两处动作：

**（1）进入 `FAILED_PERMANENT` 必须触发清理广播**：
```
判定进入 FAILED_PERMANENT
  → 记录失败原因链（1.3 节原有逻辑）
  → 新增：对该 version 当初 fan-out 的全部候选副本节点广播 DeleteByVersion
```
必须覆盖"全部候选节点"，不是"确认写成功的那几个"——控制层判定失败时并不知道数据实际落到了哪些节点，清理指令宁可对没收到数据的节点发一次空操作，也不能漏掉真正落盘的那个。

**（2）迟到的 `ReportDataDurable` 必须做状态校验后再决定是否应用**：
```
收到 ReportDataDurable(versionID, ...)
  → 检查 version 当前状态
    → 仍在"等待数据确认"：正常应用，标记 READY
    → 已是 FAILED_PERMANENT（已判死+已清理）：丢弃，不做任何状态变更
    → 已是 READY（如换协调者重试后成功）：丢弃，避免重复处理
```
不能省——迟到确认若被当成"现在写成功了"直接应用，会出现比原问题更糟的语义错乱：客户端可能早已判定写入失败并走了补偿逻辑，系统却让这份数据突然变得可查。

**（3）附带分类澄清**：控制层收到写入超时、无法确定是否真的写成功（即本节场景的触发点）时，不需要给 `FailureReason` 新增分类——按 `FailureTransient` 处理即可：数据写入内容寻址天然幂等（1.4 节 at-least-once 兜底），重试代价低，不确定时默认按"可能是暂时性问题"处理更安全。真正的 `FAILED_PERMANENT` 判定是重试预算/超时耗尽之后的事，本节两条改动处理的正是"判死之后"这个时间点遗留的物理数据和迟到消息。

**（4）清理广播需覆盖索引产物，不止 docstore 数据【新增】**：广播内容需加一项——候选节点收到后，除了清理 docstore 数据，也要检查并清掉自己本地任何跟该 version 相关的索引构建残留（不论当前是 `BUILDING` 还是其他残留状态）。索引构建的候选选择本身还有一层"版本从未判死、但候选被静默放弃"的常规情况（构建到一半失败、控制层换了下一个候选成功），这种情况不会触发本节的终态清理广播，需要候选节点自己靠本地文件 mtime 自超时兜底，详见 `coordinator-selection-and-node-liveness-design.md` 第 6 节。

---

## 2. 索引服务能力阈值归属

### 2.1 结论：声明式配置管告警，实际路由完全不查它
服务站的实际路由决策永远基于自己实时观测到的健康状态，从不查这个阈值——阈值只服务于"当前 READY 副本数够不够"这句告警语义的基线。

### 2.2 方案
- **声明式（控制层，KB 级配置）**：`IndexServingReplicaMin`，与 `DurabilityPolicy.Replicas` 同级但独立，唯一用途是触发 `DEGRADED` 告警。
- **动态（服务站，运行时）**：实际路由完全基于实时健康检查，与该阈值无关。

---

## 3. 4.4 节四个工程细节

### 3.1 连接管理
每个存储节点一个共享 gRPC channel。建立于节点首次进路由表；关闭于节点被移除路由表。

### 3.2 超时预算传播
客户端 deadline 透传，服务站算出 remaining 后设为下游 RPC deadline。重试链条用**次数上限 + 剩余预算**双重限制，谁先耗尽就停。

### 3.3 延迟预算核算
```
总预算 = 服务站路由决策开销（可忽略）
       + 网络往返 ×2
       + 存储节点检索耗时（命中索引 vs 暴力扫描，见 3.5(b) 表）
       + 故障转移预留（至少一次重试的时间片）
```
具体数字待真实环境测。

### 3.4 背压/熔断
每节点维护在途请求数；标准三态熔断（`closed`/`open`/`half-open`）。熔断状态是服务站**本地**状态，实例间不同步。

---

## 4. 一致性窗口量级约束【占位默认值，待压测替换】

| 窗口 | 占位值 | 依据 |
|---|---|---|
| 写入攒批窗口（3.6节） | 200ms 或累计 4MB，先到为准 | 取"毫秒到秒级"下限，保守控制可见延迟影响 |
| 暴力扫描临界 n（3.5(b)节） | n ≤ 5万走暴力扫描+异步补建；n > 5万直接阻塞/拒绝 | n=10万已与"等免图索引建完"同量级 |
| quorum 确认延迟 / 协调者接管超时（1.3节） | 副本本地超时 200ms | 局域网直连场景，覆盖正常 RTT 抖动 |
| epoch 恢复窗口（2.4节） | 5 秒 | 节点重启后拉取 peer localVersion 并凑 quorum 的往返+重试余量 |

这四个数字集中放进一个配置结构体（如 `ConsistencyBudget`），方便以后用真实压测数据一次性替换。

### 4.1 写入攒批的隐含风险与显式约束【新增】

攒批发生在 Raft propose **之前**，攒批缓冲区里的内容全在内存里，没有 WAL、没有持久化。若攒批节点在窗口关闭、真正调用 `ProposeCreateVersion` 之前崩溃，缓冲区内容直接丢失。

**这不是新增风险，是 1.4 节客户端 at-least-once 兜底机制本该覆盖、但之前没显式言明的场景。**

**强制约束**：攒批节点在窗口关闭、Raft commit 成功之前，**绝不能**向客户端返回任何"写入成功"的确认。ack 必须卡在 version 真正 commit 之后才发出。

在此约束下：客户端在收到确认前不会丢弃原始数据，会按 at-least-once 语义持续重试；重试因内容寻址天然幂等，代价很低。也就是说，攒批窗口只是把 1.4 节"客户端在 durable 确认前不能丢弃数据"这段责任窗口，从"写入请求到 quorum 确认"往前延长到"写入请求到攒批窗口关闭再到 Raft commit"，责任模型没变，只是变长了。

**若违反该约束**（提前 ack）：客户端会认为写入已成功并丢弃本地副本，一旦攒批节点此时崩溃，这批数据永久丢失，无法挽回——1.4 节整套 at-least-once 兜底都建立在"客户端还没被告知成功"这个前提上。

### 4.2 窗口策略：固定窗口，不做自适应【已确认】

讨论过自适应窗口（短探测期未命中并发写入则提前关闭，避免稀疏写入场景白等固定窗口）的方案，**决定暂不实现**。当前固定窗口（200ms/4MB）保持不变；写入并发密度摸清楚之后再评估是否值得做自适应。
